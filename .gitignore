# 依赖
node_modules/

# SQLite 数据库文件（运行时产物，不入库）
*.db
*.db-shm
*.db-wal

# 环境变量 / 凭证
.env
.env.*

# 密钥文件
*.key
secrets/
*.pem

# pm.py — 含明文 dev key (T+48h 轮换), 绝不入 git
pm.py
pm_*.py

# 临时快照目录
/tmp/omni-snapshot/

# 审计报告（本地工作产物）
audit-report.md

# macOS
.DS_Store
.claude/

# 自验临时脚本
.claude/hooks/_selftest.py
.claude/hooks/*.pyc
