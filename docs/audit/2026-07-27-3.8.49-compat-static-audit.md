# 3.8.49 vs 我侧血统互斥铁证表 (Step -1 静态核)
# 2026-07-27 · cg52 静态查证 · 纯本地只读 (upstream/ 两树 + dev/logic 五件), 未触 Space 未联网
# 出处: Zen 2026-07-26 "搜索查证能升级 3.8.49 吗" 令 → Step -1 (cg52 静态核) → Step 0 (K3 dev 实证)
# 铁律守: §1 upstream/ 两树只读; §2 secret 零入; §3 SSOT; 不触生产凭 ~/.omn_env

## §1 任务定位

Zen 2026-07-26 回执 4 步升级序 (Step 0 dev 24h → Step 1 prod 切 → Step 2 回滚预案),
授权 cg52 在 Step 0 前插 **Step -1 (静态互斥核)** — 给 K3 一份 "3.8.49 vs 我侧血统五件互斥铁证表",
让 K3 Step 0 实证时知盯哪条机制链 (不仅知 "没炸", 知 "机制兼容").

本档 = Step -1 产物. 纯本地只读 — 不触 Space, 不联网, 不触生产凭.

## §2 我侧血统五件清单 (dev/logic/)

| 件 | 字节 | 血统角色 | 3.8.49 升级是否动 |
|---|---|---|---|
| entrypoint.sh | 15457 | bootstrap 后主循环 (litestream strict / init 调度) | 我侧覆盖上游, 不动 |
| gate.js | 23417 | #4 OOM Patch B (CTX 字节硬拦 1.5MB) + PSK + proxyV1 | 我侧覆盖上游, 不动 |
| init-nim-keys.sh | 68693 | 25-key 探活 + real_context=200000 per-model override 注入 | 我侧覆盖上游, 不动 |
| litestream.yml | 1745 | R2 桶参数化 ${R2_BUCKET} | 我侧血统, 不动 |
| package.json | 400 | 我侧包名/血统标记 | 我侧, 不动 |

**升级径定谳 (上轮Zen令)**: 走变量切换径 (BASE_IMAGE digest 换), **不动 dev/logic 五件**.
即 — 升级上游 base 镜像 (含 3.8.49 OmniRoute Next 应用源码), 我侧血统五件仍覆盖.

## §3 3.8.49 vs 3.8.43 src/lib/modelCapabilities.ts diff 铁证

| 函数 | 3.8.43 行 | 3.8.49 行 | 改动类型 | 机制摘要 |
|---|---|---|---|---|
| `getAuthoritativeStaticContextWindow` | (无) | 188-203 | **新增** | provider→modelId→rawModel 三级权威查询, 读 modelSpecs `getAuthoritative*ContextWindow` 两 helper |
| `getMaxTokenCapabilityOverride` | (无) | 353-363 | **新增 (Issue #6524)** | 读新表 modelCapabilityOverrides 的 max_token override 作手动逃生舱 |
| `getExplicitModelOutputCap` | (无) | 366-380 | **新增** | reasoning-token-buffer clamp 用, override > synced > registry > static (四级 fallback) |
| `getResolvedModelCapabilities().contextWindow` | 354 | 423-427 | **新链前置** | `authoritativeContextWindow ?? synced.limit_context ?? registry ?? spec` — authoritative 插入首位 |
| `getResolvedModelCapabilities().maxInputTokens` | 381 `synced?.limit_input ?? contextWindow` | 453-457 | **新链** | `registry.maxInputTokens ?? authoritativeContextWindow ?? synced.limit_input ?? contextWindow` |
| `getResolvedModelCapabilities().maxOutputTokens` | synced ?? registry ?? spec | 459-466 | **加 override 前置** | `maxTokenOverride ?? synced.limit_output ?? registry ?? spec` |
| `getModelContextLimit` (我侧 200K 走径) | 436-448 | 513-525 | **函数体零改 (diff 仅签名上移)** | `override ?? resolved.contextWindow` |

## §4 我侧 real_context=200000 走径双版定位 (四大铁证)

**铁证 1 — 注入路径未变** (dev/logic/init-nim-keys.sh:888-899):
```bash
apply_context_override() {
  if sqlite3 "$_DB_PATH" \
    "INSERT OR REPLACE INTO model_context_overrides (provider, model_id, real_context, source, refreshed_at)
     VALUES ('nvidia', '$(sql_escape "$1")', $2, 'init', datetime('now'));" 2>/dev/null; then
    OVERRIDE_APPLIED=$((OVERRIDE_APPLIED+1))
  ...
}
while ...; do apply_context_override "$_M" "$_NIM_REAL_CONTEXT"; done < <(build_all_models)
```
→ 我侧 25-key 各 model 一条 `real_context=200000` 行, provider='nvidia', source='init', 入 SQLite `model_context_overrides` 表.

**铁证 2 — 表接口双版字节级一致** (`src/lib/db/modelContextOverrides.ts`):
```
$ diff upstream/omniroute-3.8.43/src/lib/db/modelContextOverrides.ts \
       upstream/omniroute-3.8.49/src/lib/db/modelContextOverrides.ts
$ echo $?
0   # 零改
```
表 schema (`provider, model_id, real_context, source, refreshed_at`) 双版字字一致. `getModelContextOverride()` 双版语义同: `return record ? record.realContext : null` (3.8.43 行 82; 3.8.49 同).

**铁证 3 — 消费点双版字节级一致** (`src/lib/modelCapabilities.ts` `getModelContextLimit` 函数体):
```ts
// 3.8.43 行 442-448 ≡ 3.8.49 行 518-525 (diff 仅模型签名 modelId? 行上移非改)
const resolved = ... getResolvedModelCapabilities(...);
// Feature 5004: a persisted override (operator-set or auto-discovered) wins over the
// static catalog / models.dev sync. `getResolvedModelCapabilities` stays override-free
// so the reconciler can compare the catalog value against provider-declared windows.
const override = getModelContextOverride(resolved.provider, resolved.model);
return override ?? resolved.contextWindow;
```
注释 "Feature 5004: persisted override wins over static catalog / models.dev sync" **双版同句** — 上游明确: override 优先静态 catalog + models.dev sync.

**铁证 4 — override 非 null 即 wins**:
我侧 override real_context=200000 ≠ null → `override ?? resolved.contextWindow` 永远返回 200000.
3.8.49 新加的 authoritativeContextWindow (静态静态表 1M), maxInputTokens 新链 (registry ?? authoritative ?? synced ?? contextWindow) **仅在 override=null 路径生效**. 我侧有 override, 这三新链 **不触**.

## §5 3.8.49 静态表 GLM-5.2 = 1M 与我侧 200K 关系判

3.8.49 `src/shared/constants/modelSpecs.ts` (`glm-5.2` spec, 行内):
```ts
"glm-5.2": {
  maxOutputTokens: 131072,
  contextWindow: 1000000,   // 1M (覆盖 models.dev 错报 128K)
  supportsThinking: true,
  supportsTools: true,
}
```
authoritative provider map 6 行 (`AUTHORITATIVE_PROVIDER_CONTEXT_WINDOWS`):
```
cloudflare-ai/@cf/zai-org/glm-5.2          → 262144
huggingface/zai-org/glm-5.2                → 262144
opencode/glm-5.2                           → 1000000
opencode-zen/glm-5.2                       → 1000000
opencode-go/glm-5.2                        → 1000000
zenmux/z-ai/glm-5.2 / glm-5.2-free         → 1000000
```
→ **nvidia 不在这 6 provider** (nvidia/z-ai/glm-5.2 provider 命中 authoritative provider map = false).

我侧生产实跑 = `nvidia/z-ai/glm-5.2` (生产 25-key NIM, gate 上行 NVIDIA 域 integrate.api.nvidia.com).
判:
- nvidia provider 不命中 AUTHORITATIVE_PROVIDER_CONTEXT_WINDOWS → authoritative provider override 不触发.
- modelId `glm-5.2` 是否命中 AUTHORITATIVE_CONTEXT_WINDOW_MODEL_IDS (含 glm-5.2/glm-5.2-high/glm-5.2-max 三条)?
  → `getAuthoritativeStaticContextWindow` (3.8.49 行 188-203) 先查 provider 级 (provider+candidate), 未中再查 modelId 级 `getAuthoritativeContextWindow(candidate)`.
  → 若 nvidia/z-ai/glm-5.2 拆出 modelId 含 "glm-5.2", 命中 static authoritative 返回 1000000 (1M).
  → 但 **1M 仅填 `resolved.contextWindow`**, 我侧 override 200000 `??` 之前 wins, 1M 不替代我的 200K.

## §6 互斥铁证表 (cg52 4 条结论)

| 机制 (3.8.49 新加) | 拦径 | 我侧血统 | 互斥判 | file:line |
|---|---|---|---|---|
| `getAuthoritativeStaticContextWindow` (1M provider 表) | 查静态目录 → 填 `resolved.contextWindow` | 我 override 200000 `??` 之前 wins | **正交** (override 优先, 静态仅 fallback) | modelCapabilities.ts:524 / 188-203 |
| `max_token` override (Issue #6524) | 查 modelCapabilityOverrides 表 → 填 `maxOutputTokens` | 我侧未注入 modelCapabilityOverrides (我只注 model_context_overrides) | **正交** (不同表不同字段 token 数) | modelCapabilities.ts:353-363 / 459 |
| `maxInputTokens` 新链 (registry ?? authoritative ?? synced ?? contextWindow) | 仅 override=null 路径生效 | 我 override 200000 非 null | **正交** (override 之前已 wins, 新链不触) | modelCapabilities.ts:453-457 |
| `getModelContextLimit` 函数体 `override ?? resolved.contextWindow` | 优先 override 再 fallback resolved.contextWindow | 我 override 200000 = 上游明确写入 wins 静态 catalog | **保护** (Feature 5004 双版同注释保 override 优先) | modelCapabilities.ts:524-525 ≡ 3.8.43:447-448 |

**四大铁证 → K3 结论前置**:
1. 我侧 `real_context=200000` override 走 `model_context_overrides` 表, 3.8.49 该表/接口/消费函数体零改 (§4 铁证 1-4).
2. 3.8.49 三新机制 (authoritative / max_token / maxInputTokens 新链) 仅在 **override=null** 时生效, 我侧有 override 不触 (§6 第 1-3 条).
3. 上游 "Feature 5004" 注释双版字字同, 明确 persisted override wins over static catalog + models.dev sync (§4 铁证 3).
4. 3.8.49 GLM-5.2 静态表 1M 与我侧 200K **正交** — 静态表填 `resolved.contextWindow`=1M, 我 override 200K `??` 之前 wins = 生产实 effective=200K (非 1M, 亦非降级).

## §7 gate.js #4 CTX guard (Patch B) 与 3.8.49 关系判

我侧 gate.js CTX guard (3.8.43 Patch B, dev/logic/gate.js:39-215):
- 拦 **content-length 字节** (非 token, 非 contextWindow token 数)
- 阈值 CTX_MAX_BYTES=1500000 (1.5MB), est_tokens=bytes÷8 ≈187500 (距 200000 budget 留 12500 余量)
- 在 PSK 后 (未认证 401 不消耗此检查) proxyV1 前 (不进上游堆, 零内存开销, 不扰 SSE)
- 返 413 + `{type:"context_length_exceeded", est_tokens, limit_bytes}`

3.8.49 升级动的是上游 base 镜像 (OmniRoute Next 应用源码),
**我侧 gate.js 是覆盖上游的 (dev/logic/gate.js 我侧血统)** — 升 base 不动我 gate.js.
判:
- gate 拦径 = 字节硬拦 = 与 contextWindow/maxInputTokens token 数机制**正交** (gate 防字节堤, 3.8.49 override 防模型 catalog 错 max_tokens).
- 升到 3.8.49 → 我 gate.js 不变 → gate 拦 1.5MB 不变, 阈值 CTX_MAX_BYTES=1500000 不变, est=187500 不变.
- gate 之后进 proxyV1 → proxyV1 转 OmniRoute Next (3.8.49) → 3.8.49 modelCapabilities.ts 走 override 路径, 我 override 200K wins.
- 无双重限制误杀险 (gate 字节 ≤ 1.5MB 放行 → 3.8.49 token override 200K 不重拦, 机制链非叠加).

## §8 两须Zen定 (Step 0 前置依赖)

上轮 cg52 已指: Step 0 "nonoke/omn 设 BASE_IMAGE=3.8.49" 前须核 dev Space 现状.
须Zen贴回 (我禁触 web UI 亦禁生产凭):

1. **nonoke/omn Space Variable `BASE_IMAGE` 现值** — 是否 = 9c9aecf (nomke/omn prod 同步设的 3.8.43 digest)?
   - 若一致 → Step 0 起点对, dev/prod 同基座可比.
   - 若不一致 → 须先同步 dev 回 9c9aecf 再起 Step 0 (否则 dev 24h 全绿≠prod 兼容, 基座漂移不可比).

2. **3.8.49 base 镜像构建令** — omn-ops/ghcr/Dockerfile 改 `ARG UPSTREAM_REF=3.8.49` build & push `ghcr.io/i3t2y/omniroute-base:3.8.49` + digest 钉锚 — Zen准否此步 cg52 跑 (omn-ops 我可触, 非 omn-merge 私库)?

## §9 Step -1 → Step 0 K3 实证盯四点 (cg52 给 K3)

K3 Step 0 (dev nonoke/omn 切 3.8.49 + 24h 观察) 时盯四点:
1. init 日志 `override: N applied, M failed` 后 `real_context=200000` 读回一致 (非被 authoritative 1M 覆盖).
2. gate.js 故意发 >1.5MB body 触发 `context_length_exceeded` 413 + est_tokens 字段 (CT guard 3.8.49 base 下仍生效, 我 gate.js 覆盖上游).
3. 25-key 探活全绿 (NIM 注册路径 3.8.49 base 下不炸).
4. litestream snapshot 仍正常 (R2 bucket=omn-data dev 桶, compaction txid 链不断).

## §10 自审, 非占位

| 检 | 结论 |
|---|---|
| 是否触 Space / 联网 / 触生产凭? | 否 (纯本地 upstream/ 只读 + dev/logic 只读) |
| 是否涉 secrets 入会话/git? | 否 |
| 是否改 git 工作树 / commit / push? | 否 (仅创建本 audit 档, 无源码改; commit 待Zen令) |
| 互斥结论是否凭推论? | 否 — file:line 铁证, diff rc=0 字节级, 注释双版同句 |
| 3.8.49 升级是否 "无风险"? | 非 — 互斥判仅静态层; K3 Step 0 dev 24h 实证须跑 (动态层: 25-key + 24h litestream + 真请求) |
| 本档是否占位 / 臆测? | 否 |

## §11 过程新发现: 3.8.49 升级可行性 3 卡口 (互斥表外, 执行径实证)

Step -1 互斥核毕后, cg52 跑第 II 阶 (3.8.49 base 镜像构建前置查) 揭示 3 卡口 — 与互斥表正交,
关乎"整体切 3.8.49 base"径是否可触自动流. 此三卡口须入 SSOT (DECISIONS), 与 §6 互斥表互补
(互斥表=若切兼容否, 三卡口=切径是否可走自动流).

**卡口 1 — 上游 GitHub diegosouzapw/OmniRoute 无 v3.8.49 tag release** (GH API 实证):
```
GET /repos/diegosouzapw/OmniRoute/releases/latest → tag_name "v3.8.48" (2026-07-13)
GET /repos/diegosouzapw/OmniRoute/tags → HEAD 列: v3.8.48/47/46/45/44/43/... (无 v3.8.49)
GET /repos/diegosouzapw/OmniRoute/git/refs/tags/v3.8.49 → HTTP 404
```
= 上游 真 release 最高 3.8.48. upstream/omniroute-3.8.49 本地源码真物非上游 tag 渠道拉.

**卡口 2 — 3.8.49 是 fork 的 release/v3.8.49 分支 (commit ce80af6), 非上游 tag release**:
audit/2026-07-22-全维度接手方案.md:69 锁: "3.8.49 `~/omn-merge/upstream/omniroute-3.8.49/` release/v3.8.49 ce80af6 定点移植来源",
audit 同档 §1.5 锁: **"禁整体升级 3.8.49"** + "合成基座 = 3.8.43 代码 + 4.2.3 行为参数 + 3.8.49 定点移植",
CLAUDE.md:23 (§1 至高): "基座 3.8.43 + 4.2.3 行为参数 + 3.8.49 定点移植".
→ 3.8.49 上游渠道定位为 **定点移植源**, 非整体切 base 之版.

**卡口 3 — upstream_check.sh 自动流最高只能到 3.8.48**:
omn-ops/scripts/upstream_check.sh:41-43 `curl /releases/latest | jq .tag_name` → latest=3.8.48 (卡口 1).
脚本基于 releases/latest 自动 build+push, 无 3.8.49 release → 自动流不可触 3.8.49.
整体切 3.8.49 base 须**手动** build (手工填 UPSTREAM_REF=3.8.49@<fork 分支 ce80af6 digest>), 绕脚本自动流.
(3.8.48 base 镜像已预构建就绪 da99fac1, 见 DECISIONS "GHCR 预构建实装" + audit/k3-review-r2-v30.md.)

## §12 升级径裁决权: Zen定 (本档不预设)

Step -1 互斥表 (§6) + 3 卡口 (§11) 揭示 3.8.49 升级 4 条径候选 (本档仅实证, 不预设裁决):
1. **定点移植径 (守 §1)** — 互斥表已核正交; 欲拿 3.8.49 某 patch 走 dev/logic/** 五件 backport, 非切 base.
2. **整体切 3.8.49 base (破 §1 基座钉锚)** — 须Zen明令改 CLAUDE.md:23/DECISIONS:83 (新裁决覆盖旧锚), 手动 build :3.8.49 fork 分支 digest 钉版.
3. **整体切 3.8.48 base (守自动流)** — 上游真 release, da99fac1 已就绪, 须 3.8.48 vs 3.8.43 互斥重核 + Step 0.
4. **不升级 (互斥表归档)** — 现 nomke 生产稳态 25-key 九段绿 rc=0, 升级非必项.

裁决权属Zen. 守 CLAUDE.md §0 "不凭记忆臆测, 不重复已锁定决策 (翻案须 Zen 明确指令)" — cg52 静态核毕, 不预设径判定谳. audit/2026-07-22-全维度接手方案.md §1.5 "禁整体升 3.8.49" 仍锁, 整体切须新明令推翻.
