# 金丝雀 单轮多发级联测试(verdict 待填)
**判裁源**: Supreme 2026-07-24 定选3重定 — 单轮聚焦多发级联, 测完即关 task#21, 不延展
**日期**: 2026-07-24
**基**: boot 2026-07-24 01:57:36 真 稳态 + 周期探活四端点全绿 + chat 真流闭环

## 重定理据(圣上原令)

| 维度 | 原金丝雀 R1-R3 | 重定版(单轮) |
|---|---|---|
| 目的 | 判 403 封禁是否解除(已由 M3 交付) | 验 v4.3.2 M1+M3 在多发下是否防级联 |
| 发间 | R1 90s / R2 同档 / R3 全量 | 连发 N 发, 发间 30s(模拟 priority 多发不至压测) |
| 模型 | glm-5.2 + pro 双模型 | glm-5.2 单模型(pro 已判死, 不碰死 key 触发风控) |
| 判读 | 四档表(全通/部分/全 403/目录可查推理被拒) | 7活key在 N 连发后是否仍 alive; 有无 429/403 级联 |
| 周期 | 72h 三轮 | 单轮, 5-10 分钟完 |
| 完成后 | T+48h 宣判 | 关 task#21, verdict 写入 audit |

## 不选 1(补跑 R1-R3) 理
三轮金丝雀设计前提 = v4.3.0 rootfix 系统(固定限流 28/3/2200 + 假健康检查 GET /v1/models 数目录项无真推理)。v4.3.2 已逐掉两根因: M1 限流随存活数动态推导(boot 实证 RPM=245 concurrent=21 alive=7) + M3 探活 POST /v1/chat/completions 真推理 ping(boot 实证 7活2死0失败)。用旧诊断流程验新系统, 结论对新系统无指导意义。M3 探活 boot 时已交付金丝雀原设"宣判": 9key 中 7alive 2dead, 对照 SSOT 四档判读表 = "部分 403 → 部分封禁, 续轮换分支"。verdict 已到位, 不须三轮 72h 重复确认。

## 不选 2(直接废弃) 理
"boot 真稳态 + 周期探活 + chat 真流" 证明了**单发**推理通。但原事件失败模式非单发 = priority 策略下**多发连发**级联崩盘(9/9 全 403)。现有验证链无一环测过此: init 探活串行单发 / 周期探活 /healthz 级存活检查 / chat 真流单轮 SSE。级联是"第3-5发时上游风控触发"行为, 单发永不测出。废弃会留与原事件根因直接相关未验证盲区。

## 已判(boot 铁证, 入判读基)
- M1 动态限流: alive=7 → RPM=245 concurrent=21 interval=244ms(随死活伸缩真链)
- M3 探活: POST /v1/chat/completions 串行 → 7alive 2dead 0failed
- 死 key#3 ##4: HTTP 403 AUTH_DEAD(NVIDIA 账户级封), 不入注册
- 7活 key: nim-01/02/05/06/07/08/09 registered

## 执行规格
- **N**: 10 发(经 gate /v1/chat/completions, OR p2c 分散选 7活 key, 每key约1-2发均衡)
- **发间**: 30s
- **模型**: z-ai/glm-5.2 单模(不碰死 key)
- **payload**: messages "你好", max_tokens 16(轻量, 避压测违规)
- **鉴权**: Authorization Bearer ${INTERNAL_PSK}(dev env ~/.omn-env-dev)
- **路径**: 经 gate :7860 → OR :20128 → combo nim-pool p2c 选活 key → NIM 推理 → SSE 回
- **判读**: 每发记 HTTP/CT/TE/body 签名 + (可选 OR 账户 == 圣上贴 boot 日志末尾 OR 响应 account 选 key)
- **级联信号**: 任何 HTTP 429(单 key RPM 限) / HTTP 403(key 翻封禁 ACCOUNT DEAD) / 5xx(上游风控触发) 即异常, 记逐条报回停测不抢修

## 纪律(SSOT §6 金丝雀监测纪律)
1. 单线程串行不并发(循环顺序发, 非 async)
2. 不重试(发失败仅记, 不补发)
3. 输出只含 key 后6位(经 gate 不见 key, 输出 HTTP/SSE/脱敏 错误体)
4. 测完即关 task#21, 不延展为周期性任务

## 预期 verdict
- 若 10连发全 200 SSE 真流, 无 429/403/5xx → v4.3.2 M1 动态限流对原事件失败模式(priority 多发级联)防护实证 → **PASS, 关 task#21**
- 若任发 429(单 key 限流) → 记档, 判 M1 限流与 per_key_rpm=35 间距偏差
- 若任发 403 → key 翻封禁 → **FAIL, 异常即停报回, 不抢修, 逐条记**

---

## 执行结果(2026-07-24 ~02:1xZ 跑毕)

经 gate /v1/chat/completions, Authorization Bearer ${INTERNAL_PSK}(dev env), 发间 30s, N=10:
```
[ 1/10] HTTP 200 5394ms CT=text/event-stream TE=chunked 流首='你好'
[ 2/10] HTTP 200 3574ms SSE 流首='你好'
[ 3/10] HTTP 200 6719ms SSE 流首='你好'
[ 4/10] HTTP 200 5134ms SSE 流首='你好'
[ 5/10] HTTP 200 6446ms SSE 流首='你好'
[ 6/10] HTTP 200 3992ms SSE 流首='你好'
[ 7/10] HTTP 200 2182ms SSE 流首='你好'
[ 8/10] HTTP 200 2927ms SSE 流首='你好'
[ 9/10] HTTP 200 3688ms SSE 流首='你好'
[10/10] HTTP 200 4227ms SSE 流首='你好'
```

**汇总**: 10/10 全 HTTP 200 + SSE chunked 真流(流首"你好"), 0 非OK, 无 429/403/5xx 级联。
- 时延 2.2-6.7s 波动(NIM 侧推理延迟正常非级联症)
- 无 key 翻封 / 无 RPM 限 / 无上游风控触发

## verdict — PASS(2026-07-24)

**v4.3.2 M1 动态限流(RPM=245/concurrent=21/interval=244ms alive=7 随死活伸缩)+ M3 真探活(POST /v1/chat/completions), 对原事件失败模式(priority 多发级联 9/9 全 403)防护实证**:
- 10连发经 gate → OR p2c 选7活 key 多发 → NIM 推理 → SSE 全真流回
- 级联盲区(第3-5 发风控触发)验通, 无任何级联信号
- 与原事件(v4.3.0 rootfix 固定限流 28/3/2200 + 假健康检查 GET /v1/models)失败模式对照, 新系统两根因逐掉真活

**对照原事件映射**:
| 项 | 原事件(v4.3.0) | 本次(v4.3.2) |
|---|---|---|
| 限流机制 | 固定 28/3/2200 → 死 key 不降配超发触风控 | M1 动态 RPM=245 随 alive=7 推导, 死 key#3/#4 已跳注册不入流 |
| 健康检查 | GET /v1/models 数目录(无真推理) | M3 POST /v1/chat/completions 真推理 ping, 7活2死0失败 |
| 多发级联 | priority 9/9 全 403 触发 | 10连发全 200 SSE, 无级联 |

## task#21 终结

按圣上裁"测完即关, 不延展为周期性任务", task#21 金丝雀单轮多发级联测试 PASS → **task#21 关**。
- 旧义 R1-R3 三轮 72h T+48h宣判已被 M3 boot 实证交付(7活2死部分封禁 verdict)+ 本轮多发级联 PASS 双替代
- 不延展周期性金丝雀(boot 探活+周期探活已续管逐 boot 真 校)

## 边界遵守
- §1: 经 gate dev only, 不直发 NIM, 不持 NIM key 值
- §2: 全 200 无异常, 不触异常即停分支
- §3: 仅 PSK 经 dev env 入 os.environ, 值零入会话(脱敏输出 HTTP/SSE/流首短串)
- §6: 单线程串行不并发 / 不重试 / 输出脱敏级联信号逐条记
- §7: 多发守速率(发间30s, 单发无重试, 无 Retry-After 触发)

## 档命运

本档入批3 commit(CLAUDE.md §7修 + audit Dockerfile累积待办 + 本金丝雀 verdict 一批, 审记仓无 origin 不 push)。task#21 task-list 状态 → completed。

---

(待续, 测完关 task#21, verdict 回填, 此档 commit 入批3)
