# omn-merge 全维度接手方案 — 任何人/AI 无上下文可直接接手

> 封档: 2026-07-22 · 编: cg52 (Claude Code × glm-5.2) · 依附: cg52 v2 任务书 + Supreme 增补令 #1
> 性质: 自包含快照。读此一篇即可提炼/修正/优化/更新/维护项目。不预设你读过任何上文。
> 时点锚: 写此篇时 UTC 2026-07-22T08:25Z 左右。金丝雀 R1(03:16Z)已过未回灌, R2(15:16Z)/R3(07-23 03:16Z)待到点。

---

## §0 一句话: 这项目在做什么

把上游 OmniRoute(LLM 网关聚合器) 改成"用一批 NVIDIA NIM key 做池、HF Space 跑、R2 做灾备持久"的永续节点。
核心目标: **25 个 NIM key 日常跑不崩、不被风控封**。
当前阶段: 调查 2026-07-21 发生的 NIM **403 账户级封禁**事件 → 定位归因 → 合成改造 → 金丝雀验证 → R3 宣判后实弹。
**不是**写新网关, 是在 fork 上做防封/截长/歧义三轴加固, 严守"合成基座"裁决(不整体回退/升级)。

---

## §1 不可协商铁律(违反=项目崩)

### 1.1 双空间隔离
| 空间 | 角色 | 版本 | key 数 | bucket | 规则 |
|---|---|---|---|---|---|
| `nomke/omn` | **生产** | 4.2.3 | 25 | `omniroute-data` | **零触碰**。不探针/不配置/不试验/不引用其日志支撑 dev 结论 |
| `nonoke/omn` | **永续 dev** | v4.3.0 | 8+1 | `omn-data` | 全部工作在此 |

- 探针**只打** `nonoke-omn.hf.space`
- 两空间日志分开取证, **禁止交叉引用结论** (第二轮误判根因的教训)

### 1.2 冻结令(T+48h 宣判前, 即 **2026-07-23 03:16 UTC 前**)
- 禁 Dataset push
- 禁 Space restart / factory reboot
- 禁改现役脚本与 gate 配置
- 金丝雀时序(授权已含, **到点执行, 不再请示**):
  - R1  07-22 **03:16 UTC** 后 — 双 key 单发
  - R2  07-22 **15:16 UTC** 后 — 同档
  - R3  07-23 **03:16 UTC** 后 — 全量 9 key → **宣判**
- 异常即停: 非预期信号 → 停, 不抢修, 报回会话

### 1.3 Secrets 纪律
- secret/token 值 **零入会话、零入文档、零入 git**。记录只写**位置**不写值
- 一律 env 占位(`${NIM_KEY}` `${R2_*}` 等)
- GitHub 令牌仅只读克隆, 禁 push; 上游公开仓库克隆不需要令牌
- 已登记例外: `~/.claude/settings.json:54` vision key(env 迁移处理中, 记位置不记值)

### 1.4 速率三准则(一切对外请求默认档)
- 并发 ≤2-3
- 缓存去重
- Retry-After 优先; 无则退避, **严禁立即重试**

### 1.5 基座裁决(不可接收的反提议)
- **合成基座** = 上游 `3.8.43 @ b729a8f` **代码基座** + `4.2.3` **行为参数** + `3.8.49` **定点移植**
- **禁整体回退 4.2.3**
- **禁整体升级 3.8.49**
- upstream/ 两棵树只读: 禁整树替换/禁直接运行/禁入生产

### 1.6 取证纪律
- 机制结论必须 **file:line 双版本对照**(3.8.43 / 3.8.49)
- 内部状态只认**日志签名**与**持久通道**(Litestream→R2、Dataset git); 探针只做最终确认, 顺序不可反
- 不人工翻更新记录

---

## §2 上游与基座(真源在哪)

### 2.1 双树
| 树 | 路径 | ref | commit | 用途 |
|---|---|---|---|---|
| 3.8.43 | `~/omn-merge/upstream/omniroute-3.8.43/` | tag v3.8.43 | `b729a8f` | **代码基座** |
| 3.8.49 | `~/omn-merge/upstream/omniroute-3.8.49/` | release/v3.8.49 | `ce80af6` | **定点移植来源** |

### 2.2 关键架构热区(两版本对照)
- combo 命名空间: 3.8.43 `services/combo.ts` → 3.8.49 拆 `services/combo/` + `resolveAutoStrategy.ts`
- `chatCore.ts`: 3.8.43 ≈4308行 vs 3.8.49 ≈4728行(+420行机制热区)
- `contextManager.ts`: 617 字节同
- `accountFallback.ts`: 3.8.43 `open-sse/services/accountFallback.ts:704` 区 (4.B 第10项发现: 403→quota_exhausted 1h 短冷却 = P0 缺陷锚点)
- Retry-After: 3.8.43 `open-sse/handlers/chatCore.ts:2360` 只解析秒数, 遇 HTTP-date `parseFloat`→NaN (p0-retry-after 补这洞)

### 2.3 四层防崩溃(上游既有, 改造要尊重)
1. Circuit Breaker
2. Cooldown-aware Retry
3. Lockout Policy
4. Account Fallback

### 2.4 三层隔离架构(本仓落地)
| 层 | 内容 | 形态 |
|---|---|---|
| 环境层 | GHCR 镜像 `ghcr.io/i3t2y/omniroute-base:stable` (sha256:9c9aecf, base=3.8.43) | 不改则避 HF docker build 冻 |
| 逻辑层 | HF Dataset (逻辑脚本) | restart 零 rebuild |
| 业务层 | 配置/key/combo | 运行时 |

---

## §3 仓拓扑(树图)

```
~/omn-merge/                          # git 仓, branch=main(7699e23), ahead nomn/main 26
├── CLAUDE.md                         # 工作宪法 v2(52行), 每会话自动加载 ★必读
├── .claude/
│   ├── settings.json                 # 护栏: 16 deny + 2 ask + PreToolUse hook (不入 git)
│   ├── hooks/secret-scan.py          # 前置拦截钩子(26行, 0.3打补时外部手落)
│   ├── hooks/_selftest.py            # 自验 chr 合成串 5/5
│   ├── _backup-20260722-000014/      # 护栏设B备份
│   └── worktrees/                    # 含 nim-pool-rebuild worktree
├── .git/hooks/pre-commit             # 扫暂存区 nvapi/PSK 明文 exit 1 (12行)
├── upstream/                         # ★只读: 3.8.43 + 3.8.49 双树
├── candidate-v4.3-reviewed/         # ★现役待接入: entrypoint-merged.sh + init-nim-keys.sh + gate.js + litestream.yml
├── baseline-4.2.3/                   # 基线保留(四件套指纹OK), 一字节不许动
├── patches/p0/                       # ★本轮新建弹药(8件, R3后接入)
├── audit/                            # ★文档链 SSOT 入口在此
├── docs/                             # 部署指南 + 永续节点方案 v1/v2 + 旧 bundle
├── archive/                          # 作废: 4.3.1 5.0 5.1
├── 4.2.3/ 4.3.1/ 5.0/ 5.1/          # 旁支(5.0/5.1 瞎生, 已作废归档)
├── old/                              # 旧版 4.1.0/4.2.2
├── cf-worker/                        # OpenConnector CF Workers 自部署
├── ghcr-tracked-Dockerfile           # 环境层 build
├── entrypoint.sh / init-nim-keys.sh / gate.js / litestream.yml  # 仓根旧版(非现役)
└── omn-merge-v4.3.0.md               # 旧大文档(97KB)
```

### 3.1 现役产物 fingerprint(接手先比对防漂移)
| 文件 | sha256前12 | 行数 |
|---|---|---|
| `candidate-v4.3-reviewed/entrypoint-merged.sh` | `bc276275fc18` | 250 |
| `candidate-v4.3-reviewed/init-nim-keys.sh` | `cea2b20eac05` | 860 |
| `candidate-v4.3-reviewed/litestream.yml` | `1563c08de199` | 29 |
| `candidate-v4.3-reviewed/gate.js` | `616047c65b61` | 520 |

### 3.2 git 远端(5个, 易混)
| remote | url | 用途 |
|---|---|---|
| `nomn` | github.com/i3t2y/n-omn | **部署源**(→HF Action→nomke/omn 生产) |
| `nog` | github.com/i3t2y/nog | n-omn 镜像支 |
| `hf` | ~/omn-hf-clone | HF local 废弃(noxke/omn v3.2.2) |
| `sp3t2y` | HF spaces/3t2y/a | 旧 space |

> branch 多: main(现)/fusion-main/fusion-main-backup/stage1-nog/stage2-hf/stage3-nomn/nomn-main-snapshot/feature/slim-monitor/worktree-nim-pool-rebuild。**别乱切**, 接手先 `git branch -vv` 看清楚。

---

## §4 文档链入口(SSOT, 接手必读顺序)

1. **主文档**: `audit/2026-07-21-nim-403-账户级封禁-归因与改造立项.md`
   - 六段: 归因修订史 / 官方机制对照 / 12项核查证据 / 4.A谱系表 / P0-P3改造规格 / 金丝雀监测记录 + 附录路径纠正/资产台账
2. **交接包**: `audit/2026-07-21-step5-验收交接包-cg52.md` v1 (③④⑤⑥暂停, ②由金丝雀吸收)
3. **时序总表**: `audit/2026-07-21-cg52-五步归档执行时序总表.md`
4. **任务书**: cg52 新方向任务书 v2 (冲突以主文档为准)
5. **本篇**(全维度接手方案, 自包含)

### 4.1 关键 audit 文件(按需)
- `2026-07-21-nonoke-omn-v4.3.0-rootfix-closure.md` — v4.3.0 init 根因闭环
- `2026-07-21-cg52-五步归档执行时序总表.md` — 五步时序
- `2026-07-20-r3plus-step5-anomaly-blocked.md` — R3+ Step5 阻卡(§六①-③已落)
- `2026-07-20-r3plus-group1v2-429-rootcause.md` — 429 根因
- `12-direction-b-hardening.md` ~ `00-inventory.md` — 历史逐步审查链

---

## §5 护栏机制(机器拦>人工守)

### 5.1 settings.json (16 deny + 2 ask, 不入 git)
```
deny:
  Bash(hf restart*) / *factory_reboot* / *reboot*factory*     # 防触 Space
  Bash(git push*)                                             # 防推
  Bash(shred*) / rm -rf /* / rm -rf ~*                        # 防删
  Bash(curl*nomke-omn.hf.space*)                              # 防探生产
  Bash(curl*nonoke-omn.hf.space*) / wget*nonoke-omn*          # 金丝雀探针走CC外(设计非漏洞)
  Read(.env* / **/.env* / **/secrets/** / **/*.key)           # 防读凭证
ask:
  Bash(git add*) / git commit*                                # 提交人工裁决
```
> pre-commit + deny git push = Dataset 公开面之前双保险闸。

### 5.2 PreToolUse hook: `.claude/hooks/secret-scan.py`
- 扫 Bash/Write/Edit 载荷, 命中 `nvapi-`(20+) / `X-Internal-PSK` 明文 / `Bearer` 明文 → **exit 2 拦截**
- env 占位 `${VAR}` / 短占位 `nvapi-xxx` 放行
- 自验: `python3 .claude/hooks/_selftest.py` 5/5 PASS(测试串全 chr() 合成, 真key不入会话)

### 5.3 pre-commit: `.git/hooks/pre-commit`
- 扫暂存区 `nvapi-[A-Za-z0-9_-]{20,}` + `X-Internal-PSK.{0,4}[:=].{0,4}[A-Za-z0-9_-]{16}` → exit 1 拒提交

### 5.4 .gitignore 关键忽略
- `.claude/` (settings.json/hooks 不入 git)
- `.env*` `*.key` `*.pem` `secrets/` `pm.py` `pm_*.py` `*.db*`

### 5.5 验护栏活态(接手第一步自检)
```bash
cd ~/omn-merge
python3 -m json.tool .claude/settings.json >/dev/null && echo JSON-OK
bash -n .git/hooks/pre-commit && echo SH-OK
python3 .claude/hooks/_selftest.py          # 应 5/5
git check-ignore -v .claude/settings.json   # 应命中 .gitignore:30 .claude/
```

---

## §6 P0 弹药(本轮已合成, R3后接入)

> 位置: `patches/p0/` 下两子目录, 各含补丁 + README + mock 验证。**弹药非上膛**, R3 宣判前不出仓不上 Space。

### 6.1 p0-retry-after (4件)
| 文件 | 作用 |
|---|---|
| `parseRetryAfter.ts` | 解 Retry-After 头两格式(秒数+HTTP-date), 补上游 `chatCore.ts:2360` 只秒数→NaN 洞 |
| `backoffAndDedup.ts` | 无头指数退避+全幅抖动(禁立即重试守门) + 请求指纹去重接口(DedupStore) |
| `verify_parseRetryAfter.js` | mock 验证 **10/10 通过**(两格式正确等待+上游洞修复+退避+去重) |
| `README.md` | 作用/证据 file:line/TBD 7条/验收红线 |

**架构师红线**: "遵守"非"重试策略" — 只解析给等待值, 403分类策略留 TBD 不实现。

### 6.2 p0-events-table (4件)
| 文件 | 作用 |
|---|---|
| `events_schema.sql` | 4列(id/ts/event_type/payload)+2索引, IF NOT EXISTS 幂等 |
| `events_write.sh` | bash 写入函数库, event_type 枚举校验+JSON转义+fail-open |
| `verify_events.py` | mock 验证 **13/13 通过**(六类枚举+§6三类不缺+特殊字符还原) |
| `README.md` | 作用/写入点清单/§6对齐/TBD/验收红线 |

**event_type 6类**: `boot_banner` `upsert_result` `cb_trip` `fallback_enter` `fallback_exit` `queue_drop`

**架构师红线**: "零新增持久通道" — 只写 `storage.sqlite`(Litestream 复制那个库), 禁另开日志文件。

### 6.3 TBD 清单(R3宣判后才填, 禁拍脑袋)
**retry-after**: TBD-POLICY-1(403冷却vs熔断) / -POLICY-2(头值硬度上限) / -PARAM-3(退避基/倍/上/抖) / -DEDUP-4(指纹算法+store)
**events-table**: TBD-INSERT-1(三类TS钩点) / -PAYLOAD-2(三类payload公约) / -RETAIN-3(保留策略) / -FAILMODE-4(fail边界)

### 6.4 R3 宣判验收对照(第五步三样)
1. ✅ 两补丁目录齐备, README 含 TBD 清单(各 7 条)
2. ✅ mock 验证证明 Retry-After 两格式正确等待(10/10)
3. ✅ events schema 写入点清单与 v2 §6 金丝雀需求对齐(熔断跳闸/罚态进出/queue_drop 三类不缺)

---

## §7 金丝雀三轮(当前在跑)

> 探针**走 CC 外终端由 Supreme 手执**(因 settings.json deny 封了 nonoke-omn curl/wget — 设计非漏洞, 探针逐轮命令由 Supreme 会话给)。
> CC 内 cg52 不触 nonoke-omn, 只做本地合成/取证。

| 轮 | 时点(UTC) | 动作 | 性质 | 状态 |
|---|---|---|---|---|
| R1 | 07-22 03:16 后 | 双 key 单发 | 监测 | 时点已过(~5h前), 结果未回灌本会话, 待归档 |
| R2 | 07-22 15:16 后 | 同档 | 监测 | 待(~7h后) |
| R3 | 07-23 03:16 后 | 全量 9 key | **裁决点** | 待(~19h后) |

**金丝雀能不能提前? 不能。** 三铁律锁死:
1. CLAUDE.md §2"到点执行"明文
2. 速率准则: 提前=缩短与上轮间隔, 撞封禁窗口污染判读
3. 签新令改时序属 Supreme 权, 非 cg52 可单方面
**建议**: 先归档 R1 读数回灌, 判 403/200/签名再定 R2 调整, 比盲发信息量大。

---

## §8 接手第一步速查表(5分钟进入工作)

```
1. cd ~/omn-merge; git branch -vv          # 确认在 main(7699e23)
2. 验护栏: 见 §5.5 四条命令
3. 读 CLAUDE.md(52行)                       # 铁律全在这
4. 读 §4 文档链(主文档→交接包→时序表)
5. 核现役 fingerprint(§3.1) 防漂移
6. 核 upstream 双树(§2.1) git rev-parse 对 commit
7. 核 P0 弹药(§6) mock 验证仍过
8. 看金丝雀日历(§7) — 在 R1/R2/R3 哪个窗
```

### 8.1 当前现役状态
- 生产 nomke/omn: 跑 v4.2.3 (sha 94325bb), RUNNING, healthz 200。**零触碰**
- dev nonoke/omn: v4.3.0, rootfix 全闭环(4补丁已应用), 七项核验过
- nomke/omn 健康判读签名: healthz200 / 根307(login重定向) / v1无psk 401 / api无basic 401(后台开)

---

## §9 已知坑(踩过的, 别再踩)

| 坑 | 教训 | 防线 |
|---|---|---|
| 两空间日志交叉引用结论 | 第二轮误判根因 | §1.1 隔离 |
| hf restart/factory reboot | 触 HF docker build 冻(7/16后免费层收紧) | deny + 环境层GHCR避build |
| secret 明文入会话/文档/git | 泄露 | secret-scan hook + pre-commit + .gitignore |
| 9 key 明文过会话 | 已发生一次, 不作二次 | 自验一律 chr() 合成串 |
| pre-commit 设B后 hook 引用不存在的py | Bash/Write/Edit 全自拦死循环 | 落文件先于挂 hook, 或外部手落 |
| `git add` 验 pre-commit | 触 ask deny | 改直接验 grep 模式, 不真 add |
| HF 免费层 docker 密推冻 | 1h推>8次/并发commit冻 | 单 commit, 等冷却/克隆 space |
| Litestream `-config` fs.NArg=0 | 传 DB 位置参崩 | entrypoint 已修不传位置参 |
| R2 Class A 100万/月限 | sync-interval 10s 估260万必触 | 降至够低(现10s待核, v4.3路线专用bucket) |
| 整体回退4.2.3 / 整体升级3.8.49 | 违基座裁决 | §1.5 不可接收 |
| 上游 ENTRYPOINT 漂移 v3.8.48 | Dockerfile 自带 ENTRYPOINT 被 candidate bootstrap 覆盖 | upstream_check.sh 制度化抓 |
| fast mode ≠ 降级小模型 | 是 Opus 加速 | — |

---

## §10 历史脉络(怎么走到今天)

1. **07-21**: NIM 403 账户级封禁事件触发。Supreme 签 cg52 v2 任务书。
2. 护栏六步落地(CLAUDE.md/settings.json/secret-scan/pre-commit/.gitignore) + vision env 迁移。
3. 4.A 谱系表 / 4.B 12项机制矩阵(第10项发现 403→quota 短冷却 P0缺陷) / 4.C 移植评估 落地。
4. **07-22**: Supreme 增补令 #1 — P0 本地合成提前起跑(两项: retry-after + events-table)。
5. p0-retry-after(10/10) + p0-events-table(13/13) 合成完, R3 后接入窗。
6. 金丝雀 R1 已到点未回灌, R2/R3 待。

### 10.1 关键根因闭环历史
- v4.3.0 init 根因 = `jq @tsv` 喂对象 + `set -e` 无护栏 = 静默崩。4补丁应用(49f787c3/151161d0), 双实证(离线 rc=5/0)。
- 增量模式⑦幂等 upsert: 下次 reboot 首次实弹 POST201→PUT200, rootfix 杜绝 combo400。
- fail-closed 三短板补齐(GATE_ADMIN_TOKEN<16 后台关 / 启动序 / wait 落空)。

---

## §11 维护操作手册

### 11.1 改护栏
- 改 settings.json 后必跑 §5.5 验 JSON-OK
- 改 secret-scan.py 后必跑 `_selftest.py` 5/5
- 改 pre-commit 后必跑 `bash -n` SH-OK

### 11.2 加新 P 补丁(仿 P0 范式)
1. `mkdir -p patches/pN/<name>/`
2. 补丁文件 + `README.md`(作用/证据file:line/TBD清单/验收红线)
3. mock 验证(禁真实端点, 本地注入值)
4. 跑验证记 通过数
5. baseline/candidate 一字节不动
6. git add/commit 走 ask

### 11.3 upstream 只读守则
- 双树当只读参照, diff 取证, 禁整树替换/直接运行/入生产
- 改动只进 patches/ 或 candidate-v4.3-reviewed/(后者 R3 解禁后)

### 11.4 取证模式
- 机制结论: 两版本 file:line 对照(3.8.43/3.8.49)
- 内部状态: 日志签名 + 持久通道(Litestream→R2, Dataset git)
- 探针: 只最终确认, nonoke-omn 走 CC 外终端

### 11.5 commit 纪律
- git add/commit 触 ask → 人工裁决, 不自动
- git push 在 deny → 禁试
- pre-commit 扫暂存区明文 → 拒
- 历史已学: 勿逐文件 push 用单 commit(避 HF 冻)

---

## §12 待决项(R3 宣判后)

| 项 | 依赖 | 动作 |
|---|---|---|
| P0 接入现役(retrofit chatCore.ts:2360) | R3 解禁令 | 改现役脚本 phase |
| 403/429 分类策略参数 | R3 金丝雀数据 | 定 TBD-POLICY-1 |
| 退避参数定标 | R3 金丝雀数据 | 定 TBD-PARAM-3 |
| 三类事件 TS 钩点定位 | R3 上游定点 | 定 TBD-INSERT-1 |
| 压缩接入 / 快照扩展 | R3 | v2 原计划 |
| 全量 9 key 验证 | R3 03:16Z | 宣判: 解封分支(RPM=1 验) 或 轮换分支(9 key 全换+scope 验收) |

### 12.1 宣判两分支
- **解封分支**: 403 非封禁 → RPM=1 网关验证 → 保持/放宽
- **轮换分支**: 9 key 全换 → scope 验收 → 重建池

---

## §13 给接手者的第一句话

> 你接的是**调查+加固阶段**, 不是新功能开发。铁律在 CLAUDE.md, 文档链在 §4, 弹药在 §6。
> 现在等金丝雀 R2(15:16 UTC)/R3(07-23 03:16 UTC)。在那之前: 零触生产、零推 Dataset、零改现役、零抢修。
> 看到非预期信号立即停手报回, 哪怕看起来是"顺便修一下"。

---

*v1 封档 2026-07-22 · cg52 编 · 自包含快照 · 与 CLAUDE.md §4 文档链交叉维护, 主文档冲突时以主文档为准*
