针对 OmniRoute 在 Hugging Face Spaces 环境下的部署迭代，以下是各主要版本的功能演进与核心优点提炼。

### **v3.3.0：上游稳定性基石**
该版本确立了应对 NVIDIA NIM 供应商特定行为的防御框架，是解决业务层 502 错误的关键起点。
*   **Per-model Context Override**：首次引入针对特定模型的上下文上限覆盖机制。通过将标称 128K 实测 32K 截断的模型强制修正为 32K，有效预防了因请求超限引发的上游 502 错误。
*   **前置请求拦截**：引入 `maxBodySizeMb` 参数，在请求发送至供应商前进行体积校验，避免超大 Payload 触发 HTTP 413 错误。
*   **端点纠正逻辑**：针对压缩（Compression）功能的 API 端点进行了适配性修正，确保长文本处理路径通畅。

### **v3.4.0：数据一致性与 SSOT 架构**
该版本通过工程化手段解决了配置漂移问题，并优化了资源占用。
*   **单一事实来源 (SSOT)**：将分散在注册、Combo 创建、增量修复三处的模型清单统一为脚本顶部的单一数组。这一改动杜绝了因模型清单不一致导致的“增量修复后 502 风暴复发”问题。
*   **静态嵌入集成**：将 Memory 功能的 `embeddingSource` 由远程（Voyage）改回静态（Static）。优点是无需外部 API Key 即可在 Hugging Face 免费层实现本地向量化存储，显著提升了隐私性与成本效益。
*   **RPM 缓冲机制**：将默认 RPM 由 40 下调至 35（留出 12.5% 缓冲），有效预防了因瞬时并发过高触发的供应商 429 限流。

### **v3.6.0：网络出口净化与自检**
该版本是针对“本地端口劫持”故障的深度修复版，重点在于网络出口的安全与透明。
*   **注册表模型清理 (Purge)**：针对 v3.8 架构重写清理逻辑。通过 API 优先、SQL 兜底的方式，彻底清除 `proxy_registry` 和 `proxy_assignments` 中的错误条目，解决了 127.0.0.1:20129 劫持问题。
*   **开机环境自检**：新增危险环境变量扫描功能。在初始化阶段主动检测并告警可能导致流量劫持的 Relay/Proxy 变量，实现了从“事后清理”向“事前预防”的转变。
*   **代理生态硬禁用**：通过强制 `export` 禁用 SOCKS5 和 1proxy 市场，确保在无需代理的环境下出站路径保持绝对纯净。

### **v3.7.0：变量对齐与语义优化**
作为当前的最优版本，它实现了脚本逻辑与 Hugging Face 托管环境的完美契合。
*   **环境变量全对齐**：修正了脚本内部变量名与 Hugging Face Variables 面板不匹配的历史遗留问题（如 `CONTEXT_LENGTH_DEFAULT` 和 `NIM_REQUEST_BODY_LIMIT`）。优点是确保用户在面板上的每一项调参都能真实下沉至数据库生效。
*   **语义化策略调整**：将 `nim-codex` 的默认策略由 `context-relay` 调整为 `round-robin`。由于查证确认 Relay 机制目前仅对 Codex 生效，此改动避免了在 NIM 供应商路径上的功能空转，使系统行为更符合预期。
*   **Litestream 兼容性增强**：在清理逻辑后加入同步延迟，确保数据库的干净状态能被 Litestream 准确捕获并回写至 R2 云端存储，防止重启后旧配置“复活”。

### **综合部署建议**
建议采用 **v3.7.0** 脚本作为最终生产方案。配合**删除 R2 旧库重建**的动作，该版本能最大限度发挥 16GB 内存优势，通过“32K Override + 自动压缩 + 变量对齐”三重手段，彻底根治超长文本导致的 502 故障，并维持极高的网络出站稳定性。

*内容由 AI 生成仅供参考*