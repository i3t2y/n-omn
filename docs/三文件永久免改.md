## 抗幻觉五规则（对话全程强制生效）

规则一：坚守知识边界——超出参数记忆、缺乏上下文支撑的问题，直接回答"不知道"，不拼凑、不猜测。

规则二：强制事实锚定——涉及具体数据、时效性事件、专业领域知识时，优先调用搜索或抓取工具，以外部信息为锚点，不直接输出参数记忆。

规则三：信息透明溯源——提供事实性断言或数据时，必须附带来源；搜索获取的信息使用规范 Markdown 格式提供引用链接。

规则四：分步逻辑推理——处理复杂推演、计算或多步骤规划时，强制采用思维链模式，拆解子步骤并透明展示推导过程，不跳跃到结论。

规则五：拒绝逻辑迎合——提问中包含事实错误、矛盾假设或不成立前提时，优先指出并纠正，不顺着错误前提继续生成。

---

## 角色与工作流约束

你是本方案的首席架构师，基于第一性原理工作。
修正、优化的最主要的目的就是适用性，哪怕以后镜像升级到omniroute最新版也可适用。源码级搜索查证omniroute更多版本，以达到适配目的，尽量避免以后再更改space中的三个文件。

进入源码级查证。目标很明确：让 Space 三个文件（Dockerfile、bootstrap.sh、README.md）做到"版本无关"，未来 OmniRoute 升级只动 GHCR 镜像和 Dataset 逻辑层。为此我必须先弄清三件事：上游官方镜像的版本发布规律与标签策略、上游 Dockerfile 的用户/工具/入口契约、以及上游数据目录与环境变量契约是否稳定。

### **查证结论：上游契约高度稳定（DATA_DIR、PORT、USER node 跨 5 个小版本未变），可以实现"Space 三文件永久免改"——核心手法是把版本号从文件内容里彻底驱逐，改由 Space 构建变量和 GHCR 标签承载；同时我必须修正上轮自己给出的一个 shell 管道 bug**

本轮查证覆盖了上游源码的三个关键位置，先摆事实，再给适配设计。

#### **源码级事实锚点**

第一，版本地貌。上游迭代极快：Docker Hub 显示 3.8.43（7-02）→ 3.8.44（7-04）→ 3.8.45（7-06）→ 3.8.46（7-07）→ 3.8.47/3.8.48（7-13），main 分支 package.json 已到 **3.8.49**，即约每周 2~3 个补丁版。标签体系为：`X.Y.Z` 不可变版本标签、`-web` 变体（含 Playwright）、浮动 `latest`（当前 digest 与 3.8.48 完全一致）、以及**每天推送的开发版 `main`**——后者绝不可用。另有前科：Issue #2685 记录过 `:latest` 一度错指到 3.2.8 的事故，所以浮动标签只能作默认值，不能作升级依据。[Docker Hub - omniroute tags](https://hub.docker.com/r/diegosouzapw/omniroute/tags) [GitHub - OmniRoute package.json](https://github.com/diegosouzapw/OmniRoute/blob/main/package.json) [GitHub - Issue #2685](https://github.com/diegosouzapw/OmniRoute/issues/2685)

第二，上游 Dockerfile 的运行时契约（main 分支 runner-base 阶段）：`DATA_DIR=/app/data`、`PORT=20128`、`HOSTNAME=0.0.0.0`、`USER node`（UID 1000）、`ENTRYPOINT ["/tmp/check-permissions.sh"]`、`CMD ["node", "dev/run-standalone.mjs"]`、基础镜像是 `node:24-trixie-slim` 且**只预装 libsecret 和 ca-certificates——没有 python3/curl/jq/litestream**。第三方留存的 3.3.2 镜像 inspect 显示四个月前契约完全相同（同样的 DATA_DIR/PORT/HOSTNAME），说明这些是上游刻意维持的稳定接口，不是我们碰运气的巧合。[GitHub - OmniRoute Dockerfile](https://github.com/diegosouzapw/OmniRoute/blob/main/Dockerfile) [docker.aityp.com - omniroute:3.3.2 inspect](https://docker.aityp.com/image/docker.io/diegosouzapw/omniroute:3.3.2)

第三，两个决定性的适配论据。其一，上游运行镜像**永远缺工具**（slim 是设计决策，builder 阶段的 python3 不进 runner），所以 bootstrap 的自愈分支不是过渡方案，而是永久必需品——这否决了"全靠 GHCR 预装、删自愈"的路线。其二，升级安全性由上游保证：镜像内置 `OMNIROUTE_MIGRATIONS_DIR=/app/migrations`，启动时自动按序应用迁移，我们的日志实证了迁移带幂等检查（"column pre-exists"、"Idempotency check"）。这意味着换基础镜像升级时，R2 恢复的旧库会被自动前滚到新 schema，逻辑层零干预。

#### **当前三文件的版本耦合点诊断**

逐一排查后，真正的耦合只有两处，都在 bootstrap.sh：逐个枚举 Dataset 文件名（未来逻辑层加文件就必须改 Space），以及未钉版本的 huggingface_hub。Dockerfile 本身已经几乎版本无关，只剩一个改进空间：把镜像名从字面量变成 ARG。README.md 我没见过内容（规则一，明说不知道），但它的职责只是 HF YAML 头（`sdk: docker`、`app_port: 7860`），与 OmniRoute 版本天然零耦合。

另外，HF 官方文档确认了一个关键机制：**Space 的 Variables 在构建时会作为 build-arg 传入**。这就是"改版本不动文件"的合法通道。[GitHub - huggingface/hub-docs](https://github.com/huggingface/hub-docs/blob/main/docs/hub/spaces-sdks-docker.md)

#### **最终版 Dockerfile（环境层）**

```dockerfile
# OmniRoute 永续节点 · 环境层（版本无关设计）
# ─────────────────────────────────────────────
# 升级 OmniRoute 的完整流程【不需要修改本文件】：
#   1. GHCR 侧以新版本上游镜像构建并推送  i3t2y/omniroute-base:X.Y.Z
#   2. Space Settings → Variables → BASE_IMAGE 改为新标签
#   3. Settings → Rebuild（构建变量变更需重建，非 Restart）
# 默认值 :stable 仅用于首次部署；浮动标签在缓存面前不具有确定性。
ARG BASE_IMAGE=ghcr.io/i3t2y/omniroute-base:stable
FROM ${BASE_IMAGE}

# root 是永久需求而非过渡：上游 runner 永远 USER node 且永远缺工具，
# bootstrap 的运行时自愈需要写权限。同时保证 BASE_IMAGE 可直接指向上游
# 官方标签（diegosouzapw/omniroute:X.Y.Z）也能起——不依赖自建镜像。
USER root

# --chmod=755 属 buildkit 标准能力（HF 文档的 build secrets 同为 buildkit 语法，
# 可证构建器支持），替代 RUN chmod，消灭对文件属主的前提假设。
COPY --chmod=755 bootstrap.sh /bootstrap.sh

EXPOSE 7860

# 探活契约：node 二进制由上游镜像保证（engines: >=22 <27，Next.js 应用），
# 7860 由 HF app_port 固定，/healthz 由逻辑层 gate.js 提供。
HEALTHCHECK --interval=30s --timeout=10s --start-period=180s --retries=3 \
  CMD node -e "require('http').get('http://127.0.0.1:7860/healthz',r=>{process.exit(r.statusCode===200?0:1)}).on('error',()=>process.exit(1))" || exit 1

ENTRYPOINT ["/bootstrap.sh"]
```

#### **最终版 bootstrap.sh v3.0（版本无关 + 自我纠错）**

先交代一处自我修正（对应规则五）：上轮我给的 `_dl()` 用 `2>&1 | sed` 做脱敏，**这个写法会让 `if _dl` 判断到的是 sed 的退出码而非 hf 的退出码**——POSIX sh 没有 PIPESTATUS，下载失败会被吞掉、误判成功。v3.0 改为落盘后回放的写法，既脱敏又保留真实退出码。

```sh
#!/bin/sh
# OmniRoute 永续节点 · 自适应引导 v3.0（版本无关）
# 与逻辑层的唯一契约：Dataset 根目录必须存在 entrypoint.sh。
# 其余一切文件名、目录结构均由逻辑层自定义，本脚本不感知。
set -e
echo "[bootstrap] >>> 启动 $(date '+%F %T') <<<"

# ── 1. 环境自愈（永久机制：上游 runner 镜像刻意不装工具链） ──
_need_install=0
for t in python3 curl pip3; do
  command -v "$t" >/dev/null 2>&1 || { echo "[bootstrap] 缺失基础工具: $t"; _need_install=1; break; }
done
command -v litestream >/dev/null 2>&1 || _need_install=1
{ command -v hf >/dev/null 2>&1 || command -v huggingface-cli >/dev/null 2>&1; } || _need_install=1

if [ "$_need_install" = "1" ]; then
  # 防线：自愈能力绑定 Debian 系。上游自 v2.x 起一直是 node:*-trixie-slim，
  # 若未来改发行版，明确报错优于静默跑偏。
  command -v apt-get >/dev/null 2>&1 || {
    echo "[bootstrap] FATAL: 非 Debian 系镜像且工具缺失，无法自愈"; exit 1; }
  echo "[bootstrap] 镜像 A 模式：正在补全环境（约 60s）..."
  apt-get update && apt-get install -y --no-install-recommends \
    curl jq python3 python3-pip sqlite3 ca-certificates && rm -rf /var/lib/apt/lists/*
  # 区间钉版：容忍 1.x 全部补丁，拒绝未来的 2.x 破坏性升级。
  # （[cli] extra 自 1.x 起不存在，CLI 已内建于主包。）
  pip3 install --no-cache-dir --break-system-packages "huggingface_hub>=1.0,<2.0"
  if ! command -v litestream >/dev/null 2>&1; then
    # 资产命名经实证：v0.5.9 的 amd64 资产为 linux-x86_64，arm64 资产为 linux-arm64。
    _a=$(uname -m | sed 's/aarch64/arm64/')
    curl -fsSL "https://github.com/benbjohnson/litestream/releases/download/v0.5.9/litestream-0.5.9-linux-${_a}.tar.gz" \
      | tar -xz -C /usr/local/bin litestream && chmod +x /usr/local/bin/litestream
  fi
  echo "[bootstrap] 环境补全完成"
else
  echo "[bootstrap] 镜像 B 模式：工具链已就绪"
fi

# ── 2. 变量校验（HF_TOKEN 可选：公共 Dataset 无需令牌） ──
[ -n "$LOGIC_BUCKET_REPO" ] || {
  echo "[bootstrap] FATAL: 缺 LOGIC_BUCKET_REPO"; exit 1; }

# ── 3. 拉取逻辑层（stderr 落盘回放脱敏，保留真实退出码） ──
echo "[bootstrap] 同步 Dataset: $LOGIC_BUCKET_REPO"
mkdir -p /tmp/logic
_dl() {
  _err=/tmp/.dl.err; : > "$_err"
  _tk=""; [ -n "$HF_TOKEN" ] && _tk="--token $HF_TOKEN"
  if command -v hf >/dev/null 2>&1; then
    hf download "$LOGIC_BUCKET_REPO" --repo-type dataset --local-dir /tmp/logic $_tk --quiet 2>"$_err"
  else
    huggingface-cli download --repo-type dataset "$LOGIC_BUCKET_REPO" --local-dir /tmp/logic $_tk --quiet 2>"$_err"
  fi
  _rc=$?
  if [ -s "$_err" ]; then
    if [ -n "$HF_TOKEN" ]; then sed "s/$HF_TOKEN/[REDACTED]/g" "$_err" >&2; else cat "$_err" >&2; fi
  fi
  return $_rc
}

if _dl; then
  # 全量注入：逻辑层增删文件无需改动本脚本
  mkdir -p /logic
  cp -a /tmp/logic/. /logic/
  chmod +x /logic/*.sh 2>/dev/null || true
  echo "[bootstrap] 逻辑注入完成"
else
  echo "[bootstrap] FATAL: Dataset 拉取失败"; exit 1
fi

rm -rf /tmp/logic
echo "[bootstrap] 移交控制权给逻辑层"
exec /logic/entrypoint.sh
```

注意一个连锁影响：注入路径从分散拷贝（/entrypoint.sh、/gate/gate.js）改为统一 `/logic/`，因此 **Dataset 侧的 entrypoint.sh 里对自身及 gate.js 等文件的引用路径要相应改为 `/logic/` 前缀**——这是逻辑层的一次性调整，免重建，改完重启即生效。

#### **配套的非文件层约定**

GHCR 侧建议同时推两类标签：`X.Y.Z` 不可变标签（升级时 Space 变量指向它，确定性缓存击穿）和 `stable` 浮动标签（仅作默认）。HF 构建器对"同标签新 digest 是否重新拉取"的缓存行为我没有文档锚点（规则一，不明说），所以升级流程一律走改 BASE_IMAGE 变量这条确定性路径。升级时的数据安全由两层既有机制背书：OmniRoute 启动自动前滚迁移（只前进不后退），Litestream 在 R2 留有升级前的备份代际——因此**升级只许向前，回滚版本必须同时回滚数据库备份**，否则旧二进制读新 schema 行为未定义。

Dataset 侧 entrypoint.sh 那处"期望 3.8.43"的版本核对，建议改为读环境变量 `EXPECTED_VERSION`（未设置则只记录不比对），值放 Space Variable 与 BASE_IMAGE 同步更新——这样版本校验也不留任何硬编码。

至此三层职责闭环：Space 文件表达"怎么引导"（永久不变），GHCR 标签与 Space 变量表达"跑哪个版本"（设置界面点选），Dataset 表达"怎么运行"（随需迭代）。按上游当前每周 2~3 个补丁版的节奏，今后每次升级的操作成本就是一次 GHCR 构建加两次界面点击，三个文件原封不动。

*内容由 AI 生成仅供参考*