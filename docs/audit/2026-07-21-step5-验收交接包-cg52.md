# Step5 六步验收交接包 — cg52 (2026-07-21, v1)

> **任务变更**: rootfix 已全闭环(见 `2026-07-21-nonoke-omn-v4.3.0-rootfix-closure.md`)。本会话起任务 = **Step5 六步验收**(audit §9 序列),承接阻塞卡 `2026-07-20-r3plus-step5-anomaly-blocked.md` 挂起的"写跳验收闭环"原始定义。combo 400 根因已根治,七项日志+双探针全绿,基线扎实。
>
> **版本锚**: 本包 v2,更新于 2026-07-21(增 §7 改道复测全崩+急停时序纠正+五疑点上游源码核查+割接议程阻塞),交接时贴此版本号防拿旧包。audit §9 为真源 SSOT,本包是执行侧裁剪。

## 0. 拓扑铁律(双空间隔离,禁交叉引用结论)

| 空间 | 角色 | 版本 | key 池 | R2 bucket | 动作边界 |
|---|---|---|---|---|---|
| **nomke/omn** | 生产 | 4.2.3 | 25 | omniroute-data | **不碰**。主路径稳定,无抢修理由。周期性 120s 丢队列是已知态,割接前不动 |
| **nonoke/omn** | 永续 dev(本任务对象) | v4.3.0 | 8 | omn-data | 全部验收在此空间 |

- 探针只打 `nonoke-omn.hf.space`
- 两空间日志分开取证,**禁止交叉引用结论**(第二轮误判根因)
- 内部状态只认日志签名,探针只做最终确认,顺序不可反(铁律)

## 1. 当前基线(已坐实,继承不动)

- init rootfix 已推送 Dataset main: `49f787c3`(init)/`151161d0`(entrypoint),普通 restart RUNNING
- 七项日志核验全过 + 双探针全绿(combo 400 根因=readonly jq `@tsv` 喂对象致 `set -e` 静默崩,upsert 从未执行;已根治)
- 新限流档首次在跑: **RPM=28 / concurrent=3 / minMs=2200 / maxWaitMs=300000**
- gate 鉴权契约: `/v1/*` 用 `X-Internal-PSK` 单头(`safeCompare(bearer,PSK)`);Authorization Bearer 值须=PSK 非 OMNIROUTE_API_KEY

## 2. Step5 六步验收序列(audit §9 真源)

### ① combo 读回终验(快验,二选一不重复)

确认 nim-pool(p2c,9 模型)/nim-codex(priority,3 模型)成员与 TIER 分档一致。

**⚠ gate 后台关**(`GATE_ADMIN_TOKEN<16` → `/api/* 404`,阻塞卡 §六②-1 T0 实证),**`经 gate /api/combos 确认`路径不可行**。

二选一:
- **(a)** 读 init `hf_snapshot` 每 boot 上传的 `combos.json`(Dataset nonoke/omni-logic) — 直读即成员快照
- **(b) 优** 跳过本步,挂第五步有意 reboot 增量模式 `existed -> PUT 200` 自证(增量分支先 GET 拿 CID 才走 PUT,本身即读回证明)

选 (b) 则本步并入第五步,不单独做。

### ② nim-pool 探针(快验)

model=nim-pool 打一发,确认路由进池内成员且非 400。

**①②过 = 写跳验收闭环原始定义闭合**(阻塞卡挂起的那个任务本身)。

### ③ 排队行为观察(行为受载,30-60min)— **割接门控输入①**

- 3-5 并发打 `nvidia/z-ai/glm-5.2`,观 300s maxWaitMs 下请求**排队→完成**全过程
- **判据**: 无生产 4.2.3 那种 120s 批量丢弃
- 新限流档(28/3/2200/300000)**首次受载**,数据同时答老问题"新参数下排队时长中位数"
- **产出**: 排队完成时长中位数 + 有无 drop — 此为判断"能否进割接议程"的关键输入①

### ④ codex priority 行为(行为受载,连发)— **割接门控输入②**

- 连续多发 nim-codex,确认**稳定钉 deepseek-v4-pro**(priority 首成员)**非轮转**
- 昨日单发探针已命中首成员,本步确认**多发下稳定性**
- **产出**: priority 多发稳定性 — 割接门控输入②

**③④ 两组数据发回本会话**,判"能否进割接议程"。

### ⑤ 稳定观察窗(被动,1-2h)

挂机跑,cg52 侧看三样:
1. 无罚态风暴(FALLBACK MODE excluded_count 不持续上涨)
2. 无 queue drop
3. Litestream txid 正常推进

窗口内无异常 → **Step5 宣告全绿,Step6/7 解锁**。

### ⑥ (全绿后)有意 reboot + 增量实弹

- **普通 restart**(factory_reboot=False)即可 — 镜像层未变,避 factory build 冻(7/16 memory 警告)。沿用上次 restart 理由
- 拉日志核对三签名:
  1. `[init] Incremental mode.`
  2. upsert 从 `POST 201` 变 `PUT 200`(幂等⑦,防 R2 restore 后撞名 400 死循环;此前从未真机跑过)
  3. `Done (incremental). v4.3.0`
- PUT 200 同时自动完成 ①的读回自证(若选了 (b) 跳过)
- 验通过 = **"重启零副作用"永续关键属性闭环**

## 3. 判读签名表(v4.3.0 健康 boot,对照 grep 30s 判健康)

横幅 v4.3.0+node → 8 keys → Resilience 读回 28/3/2200/300000 → readonly 三 GET → Compression 200 → CB reset → override 9 → health 9/9 → models 9 OK → upsert PUT(增量)/POST 201(首启) → rc=0 → Done v4.3.0

死亡点定位法 = 最后打印行与下一应有行之间(set -e 脚本无护栏 `VAR=$(cmd)` 命令替换是一等嫌疑;此案死在 readonly auto 行,已修)。

## 4. 作废结论(禁止继承,原样保留)

- "新版 init 未生效"(已证伪,部署链健康;combo 400 真因在 readonly 死点)
- "增量/首启分支选择错误"(脚本走不到分支,死在 readonly 块)
- 旧根因候选 (a)-(d) 全部
- F1"models 对象数组不符 zod 被吞"(证伪,`comboModelStepInputSchema` 合规)
- F2"更新动词 PUT→PATCH"(证伪,源码注册 PUT 无 PATCH)
- **铁律**: 内部状态只认日志签名,探针只做最终确认,顺序不可反
- ── 以下为 2026-07-21 T+48h 归档任务书补充(同格式补入,承接 pm.py 全 9 key 403 实测) ──
- "额度耗尽定性"(已证伪/降级, pm.py 9 key 全 403 "Authorization failed" 非额度见底, 实为账户级鉴权拒绝; 额度耗尽应返回 429 而非 403)
- "IP 封禁假设"(排除, 用户换 IP 测两不同 key 仍 403, IP 因素被证伪; 25 key 历史未现此状, 非 IP 维度)
- "200 RPM 提额通道/提额可解"(已作废, 403 非 RPM 限流签名, 提额不解鉴权失败; 200 RPM 通道单列保留仅作"解封分支验证"用, 非解 403 本身)

## 5. 边界与纪律

- 不碰生产 nomke/omn 任何配置
- 探针只打 nonoke-omn.hf.space
- upsert 失败即 fail-closed(return 1, set -e 下 exit 1),不静默跳过、不现场抢修
- 若命中异常分支(400/404/503 与全绿推断冲突): 立即停,不抢修,报回本会话
- 第三四步数据发回本会话判割接门控

## 6. 主线之后(不今天动手,只立项)

Step5 全绿 + 增量验证过后,nonoke/omn 才算具承接生产流量资格。割接议题届时开:
- 容量评估: cg52 生产负载从 nomke/omn(25key)迁 nonoke/omn(8key),8key 撑不撑得住生产是真实问题,可能需 key 池再分配
- PSK/端点切换顺序
- 回退预案
- 生产 4.2.3 割接前不动(周期 120s 丢队列,主路径稳,无抢修理由)

**可选 backlog(闲时,割接前空档,不抢主线)**:
- WEBHOOK_URL 重启通知(entrypoint 加几行 curl,解决"事件无感知")— 成本最低收益最直接
- 每日全量快照到 R2 独立路径(补"复制≠备份"缺口)
- gate/init 进程监督器(带 MAX_RESTARTS 语义)
- 横幅那条随 v4.3.0 已落地

---

## 7. 2026-07-21 授权两被动动作结论(裁决后回填)

裁决授权今日仅两 passive 动作(探针/重启/配置变更/轮换key/现场抢修 续冻)。两动作就地报回:

### 动作二: grep init combo payload 查 120000 超时字段(嫌疑 C)— **证伪**

- combo upsert payload 实际构造(init L113-115): `{name, strategy, models}` 仅三字段
- 全文 grep `120000|120_000|timeout|TIMEOUT|maxWait|perModelTimeout|stepTimeout|cooldown` 对 combo payload 段 **零命中**
- combo schema 无 timeout 类字段;requestQueue maxWaitMs 走 `/api/resilience` PATCH(非 combo payload),init L567 已 `NIM_MAX_WAIT_MS:-300000` 覆盖默认
- 上游源码佐证:`DEFAULT_FETCH_TIMEOUT_MS=600_000`(10min, runtimeTimeouts.ts L9);唯一 120000 默认 = `RATE_LIMIT_MAX_WAIT_MS` 默认值(settings.ts L37),已被 init 覆盖成 300000
- **结论 (C) 证伪** — 120s `exceeded 120000ms timeout` 服务端超时签名 **非 init combo payload 写入**

剩两嫌:
- **(A)** Space Secrets `REQUEST_TIMEOUT_MS=120000` env — 需你侧 HF 控制台 Space→Settings→Variables and secrets 核有无。命中则该 env 经 `getUpstreamTimeoutConfig`(runtimeTimeouts.ts L75-80)压全链 fetch/stream timeout 至 120s,即超时签名源
- **(B)** 镜像漂移(BASE_IMAGE 镜像层含旧 init 或上游 3.8.43 前带 120000 默认)— 只报告不碰代码,需 GHCR 镜像层核

### 动作一: 拉 HF 实例 runtime 日志(01:20→02:33+)闭 ⑤ — **✅三签名全绿,⑤闭**

你侧 HF 控制台 Logs 面板手拷 runtime 全量日志贴回(15:38:03 boot 起点→02:33:41)。三签名逐判:

**签名1 excluded_count 持续上涨? → 绿**
- 所有 `FALLBACK MODE excluded_count=N` 行均探针实时请求触发,非空跑累积
- 01:20:28 单 nim-codex 请求内 excluded_count 1→7(退避遍历)
- excluded_count 到高点后回降归零: 01:21:53→01:22:53 全 account `error cleared`(2+6 连续 reset)
- 非单边持涨。绿

**签名2 queue drop? → 绿**
- combo loop `All models failed`(01:04/01:06/01:11/01:14/01:16/01:18/01:20:29/01:20:53 共8次)全因:
  - `exceeded 120000ms timeout — falling back` → 524 服务端超时(疑 dsp-v4-pro 长思)
  - `403 Authorization failed` → 上游 NIM 授权拒(熔断窗内)
  - `404 Unknown error` → 上游错码(熔断窗内)
- **无** `queue dropped` / `maxWait exceeded` / 队列删除字样。maxWaitMs=300000(5min)未触
- 失败均上游侧超时/拒,非本端 queue drop。绿(120s 超时真源仍待定,非 queue drop 范畴)

**签名3 Litestream txid 推进? → 绿**
- txid 正常推进无空窗:
  - 15:38:42 db_txid=1ce/replica=1ce(R2 restore 起 boot 一次性)
  - 15:38→16:00 → 1cf→1d0→1d1→...→1d9
  - 17:06→1dd / 20:36→1e3 / 23:03→1e7
  - 01:00→1f0 / 01:10→20b / 01:20→22f / 02:00→237 / 02:06→23a / 02:26→23e
- snapshot complete 每 1h(16:00/17:00/18:00/.../02:00)稳走 8 次
- HF 休眠空窗:15:48→17:06 部分 1h 段仅 snapshot 单行 — 属 HF 免费层休眠正常态,非 txid 异常
- 绿

**⑤ 宣告闭。** 三签名全清,Step5 ⑤ 稳定观察窗过。续禁令仍守(探针/重启/配置/轮换key/现场抢修冻结),仅剩你侧三问答复 + 后续主线排序待启。

### 日志侧证 runtime 120000 超时踪迹(供嫌 A/B 定层)

runtime 日志现 120 超时签名共 5 次(全 nim-codex deepseek-v4-pro):
- 01:04:38 / 01:06:44 / 01:11:58 / 01:14:03 / 01:16:08 / 01:18:13 — 均 `exceeded 120000ms timeout — falling back` + `connection error (524)`
- 全在 01:00→01:18 nim-codex 探针时段,combo 循环单目标 dsp-v4-pro 超时
- 配合嫌(A)假设: 若 `REQUEST_TIMEOUT_MS=120000` env 设,经 runtimeTimeouts.ts L75-80 压全链 fetch/stream 至 120s,dsp-v4-pro 长思超 120s 即触 524。首发国你侧核 Space Secrets 有无此 env 即定位
- 若 (A) 空则嫌(B)镜像漂移需 GHCR 核

### 三问状态(需你侧通道答)

| 问 | 状态 | 通道 |
|---|---|---|
| ① ④#1/#2 原始状态码 | 以日志为终账不再追,④ 定格"路由顺序已证、完成性未证" | — |
| ② 8 key 同 NIM 账户数(只答账户数不贴 key 本体) | 待你侧 NIM 控制台查签发来源 | 列割接阻塞项 |
| ③ Space Secrets 有无 `REQUEST_TIMEOUT_MS=120000` | 待你侧 HF 控制台核 = 嫌疑 (A) 答案 | 同 ③ |

### 动作一结果(03:00:41 reboot 后增量实弹日志贴回)— ⑥ + ①(b) 双闭

你侧再贴一份 reboot 后 boot 全量日志(03:00:41 boot 起点)。**Step5 ⑥ 增量实弹三签名全绿 + ① combo 读回 ⑥(b)顺验自证闭合**:

**⑥ 增量三签名:**
- 签1 `[init] Incremental mode.` — ✅ 已现(走增量分支非首启)
- 签2 upsert PUT 200(非 POST 201)— ✅ 双双已现:
  ```
  upsert nim-pool: existed -> PUT combos/6ba3a514-83c2-413d-991f-2e5c78c8088a HTTP 200
  upsert nim-codex: existed -> PUT combos/95a25daa-618b-4067-a816-b66cd19286c2 HTTP 200
  ```
  幂等⑦首次真机实跑成功。combo DB 读了真 CID 走 PUT(非撞名 POST)
- 签3 `Done (incremental). v4.3.0` — ✅ 已现,init rc=0

**①(b) combo 读回自证(顺 ⑥ 闭合):**
增量分支先 GET /api/combos 拿 CID,GET 响应即读回:
```
GET /api/combos HTTP 200 :: combos=[6ba3a514-83c2-413d-991f-2e5c78c8088a nim-pool p2c; 95a25daa-618b-4067-a816-b66cd19286c2 nim-codex priority;]
```
- nim-pool = p2c ✓ / nim-codex = priority ✓ — 策略与 TIER 分档一致
- 两个 combo CID 落定,combo 读回终验 ①(b) 路 ⑥ 顺验自动闭合

**txid R2 一致性核:** boot `db_txid=0 / replica_txid=23e` → R2 restore 接续上一实例 02:33 last txid=23e 吻合 → 03:01:25 retention 23f → 03:02:00 compaction 240-241。跨实例 R2 restore 连续无失.

**Step5 ⑤⑥①②③ 五项绿,剩:**
- ④ priority 完成性复测(三前置:客户端超时≥360s+原始态落盘 / 120s 服务端超时定层消除对齐 maxWaitMs=300s / 风控感知节奏3发足矣单线程发间≥90s prompt压短)
- 割接阻塞(8 key 账户数 + Secrets 有无 REQUEST_TIMEOUT_MS)

**待确认:** 本次 03:00:41 reboot 谁触发?续禁令守"不重启",若你侧有意触发=⑥ 合一弹时序对齐(时序原排⑤闭→三问齐→④前置齐→打这一弹)。但 ③④前置未齐即先打⑥,时序跳了。需你侧确认意图(若无意识触发则查 HF 自动重启源).

### ④ 复测实弹(2026-07-21T03:16Z,裁决放行后执行)— 三发全崩,停,不自结论

裁决规格: 单线程 3 发, prompt "1+1=?". 发间 ≥90s(实测 95s), 客户端超时 ≥120s(实测 120s), 不重试不并发, 落盘原始 status+body model+finish_reason+dur。
前置基线: 03:16:25 healthz `http=200 dur=1.9s active=0 tokens=28` — RUNNING 干净(重启已过 ~15min,00:00 reboot 后基线 active=0/CB reset/8 key 清罚态)。首发无唤醒延迟豁免需求(未休眠)。

**三发原始 body(完整落盘 `/tmp/codex_body_{1,2,3}.json`)**:
```
发#1 (03:16:25, dur=19s, http=404): {"error":{"message":"[nvidia/z-ai/glm-5.2] [403]: {\"status\":403,\"title\":\"Forbidden\",\"detail\":\"Authorization failed\"} (reset after 3s)"}}
发#2 (03:18:19, dur=2s,  http=403): {"error":{"message":"[nvidia/deepseek-ai/deepseek-v4-pro] [403]: {\"status\":403,\"title\":\"Forbidden\",\"detail\":\"Authorization failed\"} (reset after 3s)"}}
发#3 (03:19:56, dur=3s,  http=404): {"error":{"message":"[nvidia/deepseek-ai/deepseek-v4-pro] [404]: Unknown error (reset after 3s)"}}
```

**判读(只读,不抢修)**:
1. **违反裁决#"首发命中 deepseek-v4-pro"**: 发#1 路由痕迹落 glm-5.2 非 deepseek 首成员。但发#1 dur=19s 走完 retry 链 → 推断 priority 首训 deepseek 先 403 fallback 到 glm-5.2 仍 403。**非 route bug, 是 403 fallback 链痕迹**(待裁决确认)。
2. **body 非 choices 数组**: 三发均 error 对象, 解析时 model/finish_reason 落 err 字段非正式完成。**三发零完成**。
3. **三发全非 200**: 违反#"3/3 HTTP 200 正常完成"。**④ 未过**。

**根因定位(关键)**:
- 三发上游响应体均含 `detail: "Authorization failed"` — **build.nvidia.com(NIM 上游)自身鉴权拒**, 非 OmniRoute 流控、非 gating、非 PSK(PSK 过 → OmniRoute 路由 → 分发 nvidia/* → 才拿到上游 NIM 的 403)。
- `reset after 3s` = OmniRoute 给上游 403 标的内部冷却。
- 即: **8 个 NIM key 当前集体上游鉴权失败**(短窗风控击中鉴权层)。
- 与 03:00 boot 时"8 keys 全注册 + 9 模型 health 9/9 available"矛盾 → boot 健康检查过, 15 分钟后鉴权全败 → 上游 NIM 风控踢入短窗鉴权拒。
- **与嫌(A) REQUEST_TIMEOUT_MS=120000 无关**: 本次 prompt 极短 dur ≤19s, 未触 120s 服务端超时。本次纯上游 NIM 鉴权风控。
- **发#3 404** = 上游 NIM 对连续鉴权失败回 404(OmniRoute 透传), 非熔断器跳闸(OmniRoute 侧熔断器是 5 consecutive → 30min, 此处上游端 403/404 非 OmniRoute 熔断)。

**裁决约束遵守**: "任一漂移(非deepseek)或非200 → 停, 贴日志不自结论"。现停。三项异常(路由痕非首成员 + body 非正常 + 全非 200)报回待裁决。

### ④ 改道复测(06:13 UTC, 裁决第三步放行)— 三发全崩, 急停, 时间轴纠正

**时间轴纠正(user 侧硬两名错标 vs 实际 UTC)**:
- user 称"当前本地 11:19=UTC 03:19" → 实际 `now=07:19 UTC=本地 15:19`
- user 称"熔断盲窗剩约 27 分钟(至 03:46:44 UTC)" → 实际该窗 **早过 3.5h**(改道三发 06:13-06:16 打时熔断理论已到期 2.5h 前)
- 即: **三发打在熔断窗后, 仍全崩 → 排除网关熔断, 纯上游账户级持续锁定**。坐实"病根上游不在网关"(网关 CB reset 03:00 + 罚态清, 上游状态原样保留穿透重启).
- Monitor killed 是检测到 `改道复测结束` 完成信号正常收尾, 非拦截. PID 1734884 早于急停令 06:16:34 UTC 自然退出(三发全打完), curl 无残留.

**三发原始 body(完整落盘 `/tmp/reroute_body_{1,2,3}.json`, 全作废不判读不补救)**:
```
发#1 (06:13:00 UTC, dur=19s, http=403): [nvidia/z-ai/glm-5.2]            [403] Authorization failed (reset after 3s)
发#2 (06:14:54 UTC, dur=2s,  http=403): [nvidia/deepseek-ai/deepseek-v4-pro] [403] Authorization failed (reset after 3s)
发#3 (06:16:31 UTC, dur=3s,  http=404): [nvidia/deepseek-ai/deepseek-v4-pro] [404] Unknown error (reset after 2s)
```
三发全非 200, combo 链从未完成. pro 八 key 轮完(6 个 404 + 2 个 403), glm-5.2/gpt-oss 撞 90022b29/004e923a 这对 403 key 即"all 8 rate limited"(前 6 key pro 阶段进同请求内冷却污染后两模型 fallback 池).

**根因层进(两轮逐 key 对照)**: 01:20 vs 06:13(本地 03:16)逐 key 一致:
- 6 账户 404: 74963de9/23612c30/040ec5a2/45066307/0301760d/d141f412
- 2 账户 403: 90022b29/004e923a
- 随机风控打不出此确定性分布 → 上游逐账户持续状态. 穿透 03:00 重启(网关 reset/罚态清, 上游原样) = 病根上游坐实.
- 三个分账户事实: 90022b29/004e923a 全模型 403 已持续 ≥2h(倾向封禁/吊销); 其余 6 账户 flash 可用(02:05 实证) pro 一律 404; pro 在 15:43 经 45066307 成功(26.9s)→ 15:43→01:20 间 pro 权限全账户消失; init 03:01 健康 pro "available" 仍报 → NIM 目录可查 ≠ 推理权限在手, 目录信号今后打折.
- **裁决侧自我修正入册**: flash 通(02:05)≠ pro 通. 放行令基于 02:05 flash 过时锚点, pro 不成立, 裁决侧承担这笔.

**④ 处置**: pro 全 8 账户不可服务(404 即时, 与 120s 超时无关, 短输出绕行无效). **④ 重新冻结, 阻塞解除条件改"上游 pro 恢复"**. 深问题: nim-codex priority 首成员深度绑 pro, pro 永久性消失则 TIER 设计需修订(换首成员或接受降级链) = 割接议程输入.

**诊断探针(06:08 UTC, 改道前三发前)**: glm-5.2 直打绕 combo `http=200 dur=4s model=z-ai/glm-5.2 finish_reason=stop content="1 + 1 = 2"` → glm-5.2 稳定可用(02:05+06:08 跨 4h 双成功), pro 单独被风控. 但**注意**: 诊断 200 在 pro 仍锁前证 glm-5.2 链路健康, 非 combo 链可用 — combo 模型=nim-codex 必经 pro 首训, pro 403 后 fallback glm-5.2 但"all 8 rate limited"污染 fallback 池, glm-5.2 仍触 403(发#1 dur=19s 链条完整证明此 point).

**时序令(按实际 UTC 校正)**:
1. 现在(急停完): 零探针, 停火
2. 本地 15:50 前(user 称 11:50, 按实际已是 15:19+, 故 ~30min 停火窗): 盲窗期零探针(虽熔断理论已到期, 上游 pro 持续锁定未解, 停火仍守避免再加罚态/误判网关)
3. 本地 15:50 后: 授权一发直打 `nvidia/deepseek-ai/deepseek-v4-pro` 绕 combo, 极短 prompt, 客户端超时 120s, 原始字段落盘. 200→pro 恢复重排三发; 404 依旧→pro 判上游死, ④ 改道议题开启
4. cg52 零动作待命至 15:50

**割接议程阻塞项(新增)**: 上游 NIM 单账户风控传染 combo 完成性. 割接前必评:
- 生产 nomke/omn 25 key 是否类似账户级风控分布?
- nonoke/omn 8 账户中 2 个(90022b29/004e923a)已 403 ≥2h, 永久封禁?
- combo 设计是否需"账户级熔断"(单账户连续 403 自动排除出 fallback 池)?

**待 user 裁决三问(改道后重排)**:
1. 发#1 route 落 glm-5.2 非 deepseek — priority 首训未钉死?还是 403 fallback 痕迹?(已基本判: fallback 链痕迹, priority 首训日志层坐实, 待最终裁决确认)
2. 上游 NIM 当下集体 403 Authorization failed — 等冷却再复测(15:50 后一发 pro 直打诊断)/今日停 ④ 待明日干净窗?
3. ~~是否单发 glm-5.2 区分上游整体 down / 仅 deepseek 风控?~~ —— **已执行(06:08), 结论: glm-5.2 可用, pro 单独风控**. 阻塞源从网关转为上游 confirmed.

### 五疑点上游源码核查(2026-07-21, cg52 离线只读, 续禁令守)

**职责边界(查前澄清, 防张冠李戴)**:
- `gate.js`(HF 接收层, 守 PSK, 本地 429 tryAcquire)= omn-merge 自有, **可改**
- `init-nim-keys.sh`(boot PUT combo + PATCH Resilience + 健康检查)= omn-merge 自有, **可改**
- `~/3.8.43`(OmniRoute 上游真源 b729a8f, combo fallback/key 轮询/403 处理/真健康检查源)= **只读不改**(改断升级同步)

**你怀疑的"脚本 proxy/route/key selector/403 处理" = 此层在 OmniRoute 上游, 非自有**.

| 疑点 | 结论 | file:line 证据 |
|---|---|---|
| 1 重试风暴(403 带错继续试/8 key 全轮自杀) | **否, 已正确处理** | `comboResolver.ts:75` priority 钉首训 `return normalized[0]`; `:96` getComboFallbacks 模型级降级 slice ±index. 非同 key 带错试他模型 — combo 降级模型级非 key 级 |
| 2 Retry-After 识别 | **是, 已识别+尊重** | `cooldownAwareRetry.ts:90` getCooldownAwareRetryDecision 读 retry-after; `:77` computeClosestRetryAfter 算 waitMs=retryTimeMs-Date.now(); `:101` 超阈值 shouldRetry=false. 403 实走 `accountFallback.ts:684` status===403→"quota_exhausted"(1h 冷却) |
| 3 并发竞争 key 锁死(8 key 滚雪球) | **部分存疑** | combo 级 `comboResolver.ts:32` roundRobinCounters 持久计数器(模型级旋转). 连接级 key selector 算法在 accountFallback.ts(1789 行)未完全定位(随机 vs 顺序), 需更细读 |
| 4 健康检查真伪(目录可查 ≠ 推理权) | **是, 确认缺陷** | `init-nim-keys.sh:247` check_nim_model_health 仅 `GET integrate.api.nvidia.com/v1/models` + `.data[]?.id` 数目录项, **无真推理 ping**. 03:01 报 "9/9 available" = 目录可查 ≠ 推理权. pro 15:43→01:20 间推理权全账户失但目录仍列 → 误把死模型导流诱罚. **supreme 第四步坐实, 目录信号打折入册** |
| 5 Header 透传(X-Internal-PSK 泄上游致 403) | **否, 已白名单** | `validation/headers.ts:84` buildBearerHeaders 仅 `{Content-Type, Authorization: Bearer ${key}}` + applyCustomUserAgent. 显式白名单非 `{...req.headers, Authorization}` 透传. PSK 不泄 NVIDIA. 03:16 的 403 "Authorization failed" 是 NVIDIA 自身账户鉴权拒非 header 污染 |

**403 实际分类(关键头绪)**: 日志 `detail:"Authorization failed"` → `accountFallback.ts:243` 正则 `/\bauthentication\s+(?:failed|error|required)\b/i` 命中. L233-242 注释: 此属纯鉴权失败 = key/token 本身坏, 非模型可用性问题, regex-based combo fallback 被抑制(不当 MODEL_ACCESS_DENIED). L1519 403+apikey 类仍 buildRetryableFallback(AUTH_ERROR) 走 fallback. L684 403→"quota_exhausted"(1h) 与 L1519 AUTH_ERROR 两条分类路径并存. **定局**: 403 是上游账户级状态, 非脚本 header/重试诱发.

**综合归因(外+内并存)**:
- **主因(外)**: 上游 8 账户级持续锁定(2 个 90022b29/004e923a 全模型 403 ≥2h 封禁倾向 + 6 个 pro 404), 穿透 03:00 重启(网关 reset/罚态清上游原样), 熔断到期 2.5h 后(06:13)仍全崩 → 排除网关熔断, **纯上游账户级风控**, 非脚本诱发
- **次因(内, 贡献)**: 疑点4 健康检查流于形式, 把死模型标可用导流, combo 降级连累 fallback 模型也触罚. **脚本有责任但非根因** — supreme 判读"主因上游脚本有贡献"成立

**修复权边界**:
- 疑点1/2/5 核心逻辑在上游 **不改**(改断升级同步)
- 疑点4 健康检查在 omn-merge 自有 init **可补**(唯一能动内部修复点)
- gate.js 限流加固 = 可改但本轮非焦点

### 后续主线排序(不今日动手)

1. ⑤ 闭 ✅ / ⑥ 闭 ✅ / ① 闭 ✅(三签名+读回自证全绿)
2. 三问齐(② 账户数 + ③ Secrets 有无 REQUEST_TIMEOUT_MS) — 仍待你侧
3. ④ 复测前置三条件齐:客户端超时 ≥360s 且原始状态落盘 + 120s 服务端超时定层消除(对齐 maxWaitMs=300s)+ 风控感知节奏(3 发足矣单线程发间 ≥90s prompt 压短)
4. ④ 复测判据: 3 发全命中 deepseek-v4-pro 且全完成 → priority 多发稳定闭 ④
5. **⏸ ④ 首轮全崩(2026-07-21T03:16Z): 上游 NIM 集体 Authorization failed(403/404) 非基线问题非 ④ 设计缺陷, 待 Zen另发裁决定停测/复测/诊断路径**
6. **五疑点源码核查(2026-07-21 cg52)**: 主因外(账户级风控穿透重启+熔断到期仍崩)+次因内(健康检查流于形式疑点4). 内部唯一可补 = init 健康检查加真推理 ping(降死模型导流诱罚), 待 supreme 授权改 init 推送
7. **割接议程阻塞项**: 上游账户级风控传染 combo 完成性(8 账户中 2 封禁倾向/pro 全账户 404). 割接前必评: nomke 25 key 类似分布? nonoke 2 账户永久封禁? combo 设计需账户级熔断? + TIER 重议(pro 永没则 nim-codex 首成员换 glm-5.2 或接受降级链)
8. **supreme 五步建议可行性**: 改 combo 首成员 pro→glm-5.2 可(init PUT改顺序); 强制 403 后 key 冷却 300s **不可**(逻辑上游只读); 清 Header **已自然达成**(buildBearerHeaders 白名单); 降 RPM=1 单key验证 可(env NIM_FIXED_RPM=1)
9. **续禁令守**: 本轮全离线只读查源码零探针零重启零改配置, 待 supreme 授权(NIM 控制台状态/改 init 单key验证)才动

---

*v1 封档 2026-07-21 · 承 rootfix-closure §9 · cg52 执行 · ③④ 为割接门控输入发回本会话 · 普通restart避build冻 · 双空间隔离铁律续守*
