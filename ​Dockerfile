FROM diegosouzapw/omniroute:latest

ENV PORT=7860
ENV DATA_DIR=/data
ENV REQUEST_TIMEOUT_MS=120000

COPY init-nim-keys.sh /app/init-nim-keys.sh
COPY entrypoint.sh /app/entrypoint.sh
RUN chmod +x /app/init-nim-keys.sh /app/entrypoint.sh

EXPOSE 7860
VOLUME ["/data"]

CMD ["/app/entrypoint.sh"]
