五项规则默念完毕。基于规则四（逻辑拆解）与规则二（事实锚定），我从提供书签摘要、历次迭代记录以及本次实测核查中，提炼出 OmniRoute 部署方案各版本的优点。

请注意：由于书签摘要仅包含 v1.2 的片段，而 1.2.x 版本的演进主要体现在本次对话的实测与脚本开发中，以下提炼严格区分了“架构设计优点”与“工程落地优点”。

### **OmniRoute 部署方案版本优点提炼**

#### **v1.2.0：多层防御架构设计（核心：安全性与分流）**
该版本确立了“安全第一”的架构基准，通过引入外部组件解决暴露面问题。
*   **多层鉴权体系**：通过 Cloudflare Worker 作为边界层，实现了 `Client -> Worker` 和 `Worker -> OmniRoute` 的双重鉴权（PSK 校验），彻底隐藏了后端的 NIM Keys。
*   **前置流量分流**：引入 Manifest Cloud 作为流量裁判，将简单请求导向免费模型，复杂请求才流向 NIM，有效降低了主力 API 的负载压力。
*   **入口收缩**：通过绑定自定义域名并禁用 `workers.dev` 子域，配合 WAF 规则，极大缩小了被扫描和攻击的表面积。
*   **路由分档理念**：首次提出 Simple/Standard/Complex/Reasoning 四档路由，实现了资源的按需分配。

#### **v1.2.1 ~ v1.2.7：策略优化与风控规避（核心：行为规范）**
根据书签摘要中的“调用行为规范”片段，这一系列版本重点在于对抗上游风控。
*   **调用行为建模**：强调时间分布、模型分散和 Prompt 多样性，通过模拟人类调用行为来降低被 NVIDIA 封号的风险。
*   **监控指标细化**：新增“跨 Key 行为相似度”监控，从数据层面预防集群特征被识别。
*   **容灾备份增强**：确立了“HF Space + VPS 冷备”的双机方案，提升了系统的可用性。

#### **v1.2.8：自动化部署与事实锚定（核心：工程化与鲁棒性）**
这是本次对话中最新达成的版本，其优点在于将之前的“理论配置”转化为“实测代码”。
*   **全自动初始化（init-nim-keys.sh）**：
    *   **幂等性执行**：支持容器重建后自动检查并补全配置，避免重复添加导致的混乱。
    *   **单变量多 Key 解析**：解决了 HF Space Secrets 填写繁琐的问题，支持一个变量存放数十个 Key。
*   **Resilience 配置精确化**：
    *   **基于源码的字段名修复**：纠正了之前版本中对 API 字段名（如 `requestsPerMinute`）的猜测，确保配置真实生效。
    *   **实测数值对齐**：基于 40 RPM 的官方限额，推导出 35 RPM + 1800ms 间隔的“安全缓冲区”数值。
*   **适配最新安全机制**：
    *   **Cookie 鉴权闭环**：针对 OmniRoute v3.6.8+ 强制管理 API 鉴权的变更，脚本自动完成登录获取 Cookie 的动作，确保在最新版环境下依然可用。
*   **数据库稳定性保障**：
    *   **日志行数限制**：引入 `CALL_LOGS_TABLE_MAX_ROWS`，有效防止 HF Space 这种受限环境下的 SQLite 膨胀崩溃。

---

### **各版本演进逻辑总结**

| 维度 | v1.2.0 (架构期) | v1.2.1~1.2.7 (策略期) | v1.2.8 (落地期) |
| :--- | :--- | :--- | :--- |
| **解决重心** | 解决“能不能用”和“安不安全” | 解决“怎么用不被封” | 解决“怎么快速部署”和“配置是否生效” |
| **主要优点** | 多层反代、PSK 鉴权、流量分流 | 行为去特征化、多机冷备 | 自动化脚本、源码级字段对齐、实测数值 |
| **交付形式** | 架构图与 Nginx 配置片段 | 调用规范与监控指标清单 | 幂等 Bash 脚本与实测配置基线 |

**当前状态声明**：v1.2.8 已完成对 NIM 速率保护、Resilience Profile 以及自动化脚本的核实与落地，已具备一键部署的完整工程基础。

*内容由 AI 生成仅供参考*