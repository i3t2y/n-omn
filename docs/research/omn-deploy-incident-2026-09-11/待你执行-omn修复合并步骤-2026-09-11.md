# omn 修复 · 执行记录与待办（2026-09-11）

## 一、根因（已定案，行级证据）

`n-omn` 自 2026-09-10 起 HF 重启循环，根因是**部署清单漏件**，不是别的。

`logic/` 实含 **8 件业务文件**（已从仓库实读）：

```
entrypoint.sh  gate.js  helper.sh  init-nim-keys.sh
omn_redact.py  omn_scheduler.py  package.json  policy-guard.js
```

三处硬编码清单只列了 **7 件 + flaretunnel = 8 项**，同漏 `policy-guard.js`：

| 位置 | 行 | 作用 |
|---|---|---|
| `.github/workflows/sync-logic-xnexus.yml` | 58–59 | CI 上传 |
| `.github/workflows/sync-logic-xnexus.yml` | 104–105 | CI 回读校验 |
| `space/start.sh` | 60–61 | boot 拉取 |

链：`gate.js:22` 顶层 `require('./policy-guard.js')` → 桶内缺件 → gate 启动即崩 →
`entrypoint` 视 gate 死为致命（STRICT）→ 全停 → HF 拉起 → 循环。

**为什么 CI 一直绿**：上传与校验共用同一份清单，只验「清单内文件都对」，
从不验「清单本身全不全」→ 自我确认偏误。

> `start.sh:52` 注释早写着「9 件（8 业务 + flaretunnel）」——**注释是对的、代码漏了**。
> `logic/policy-guard.js` 本身在仓库里是好的（17249 B，sha256 `313177ce…` 与权威源一致），
> 缺的只是「清单没列它」。

## 二、已完成（本轮）

- [x] 根因定位到行级（3 处清单 + 依赖关系 + 假绿成因）
- [x] 补丁写好并验证：清单与 `logic/` 实际文件集逐项对齐；YAML / `sh -n` / 内嵌 python 语法全通过
- [x] 分支推送到**两侧**（内容同源、SHA 相同）：
  - CNB `nexus.zen/omn`：`fix-omn-deploy-bucket` (`ca83bd4`)、`fix-omn-deploy-boot` (`d475260`)
  - GitHub `i3t2y/n-omn`：同名两分支（GitHub compare 已核对：`+6/-4`、`+3/-2`）
- [x] CNB PR 已开：**#14**（Bucket 侧）、**#15**（boot 侧）
- [ ] GitHub PR：**开不了** —— GitHub 集成 token 只读（`403 Resource not accessible by integration`）。
      分支已推上去，可用下面链接建 PR，或直接合分支。

## 三、待办：按序执行，**别颠倒**

### 第 1 步 · Bucket 侧（低风险，不重启 Space）

合并 `fix-omn-deploy-bucket`（改 `sync-logic-xnexus.yml` 两处清单 8 → 9）

- GitHub 建 PR：https://github.com/i3t2y/n-omn/pull/new/fix-omn-deploy-bucket
- 或 CNB PR #14

合并后等 `sync-logic-xnexus` Actions 变绿（约 1–2 分钟）。此步**只更新 Bucket，不重启容器**。

### 第 2 步 · boot 侧（会触发 HF 自动 Rebuild）

**第 1 步绿了之后**再合并 `fix-omn-deploy-boot`（改 `space/start.sh` 清单 8 → 9）

- GitHub 建 PR：https://github.com/i3t2y/n-omn/pull/new/fix-omn-deploy-boot
- 或 CNB PR #15

合并 → `sync-space-xnexus` → 推 Space 骨架 → **HF 自动 Rebuild** → 容器重启 → 服务恢复。
若 HF 未自动拉起，手动 Restart 一次。

**为什么必须分明步**：`start.sh` 是 boot 时的「要哪些件」清单。桶里 `manifest.json` 必须先含
`policy-guard.js`，否则新 boot 会报 `[start] FATAL: manifest 缺 policy-guard.js` 退出
（下个 boot 自愈，但会多几次失败）。

## 四、成功标志

1. `sync-logic-xnexus` 日志出现：`[upload] 9 文件 + manifest.json`、`[verify] … 9 件 sha256 全对`
2. Space 重启后 boot 日志出现：`[start] Bucket 校验通过 (n-omn@… 9 件 sha256 全对)`
3. 随后 `gate listening` + `healthz 200`，不再循环

## 五、两边 SHA 一致性的注意点

CNB 与 GitHub 的 `main` 目前同为 `cd16825`。若一侧合 PR、另一侧也单独合，
会产生**两个不同的 merge commit**，两边 SHA 就分叉了（内容相同、历史不同）。

最干净：**在一侧合并 → 另一侧用快进（fast-forward）推**，两边保持同一 SHA。

## 六、另有一件待你拍板

`eval/` 公开泄漏：`i3t2y/nexus`（公开仓）里 `eval/heldout.json` 的 4 例标准答案
可被任何人 / 任何 NPC 匿名读到 → 河图「防过拟合」保险丝失效。
处置三选一见《建议书》P0.5。**未动**，等你决定。
