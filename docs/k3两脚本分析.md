### **裁 (b) gate 前置拦截为主修，外加一项立即执行的回归修复——4.3.2 的 entrypoint 丢了 4.2.3 原有的 `NODE_OPTIONS=--max-old-space-size=4096`，这才是"生产不死、dev 崩盘"的真正分水岭，而非堆压时机运气。(a) 降级为下一基镜像周期的可选加固，(c) 否决，audit 必落。**

三份文件交叉读完后，你的分析里有三个事实点需要修正或补充，其中第一个直接改变裁决的优先级排序。

#### **一、新事实 1：崩盘差异是配置回归，不是时机**

对比两份 entrypoint 的上游启动行，证据是行级的。4.2.3（omn-4.2.3.md → entrypoint.sh）启动 OmniRoute 时内联注入了堆上限，且启 gate 前又全局 export 了一次：

```sh
NODE_OPTIONS="--max-old-space-size=4096" \
node /app/server.js --log &
```

而 4.3.2（omn-4.3.2.md → omn-logic\entrypoint.sh）的启动行是裸的 `cd /app; node server.js &`，gate 也是裸的 `node /logic/gate.js &`。dev 侧的 GC 日志自我佐证了后果：`Mark-Compact 1015.9 (1028.2) MB` 到 `1023.2 (1031.6) MB` 后触顶——dev 的有效堆上限约为 1GB 的 Node 默认值。所以同一条 #4 链，生产用 4GB 扛过了 25 次同体转发，dev 用 1GB 在第 25 次前后暴毙。"崩不崩看堆压时机"应修正为"崩不崩看 4.2.3 有没有把 4096 带过来"。这是一个一行可修的回归，且是整个修复方案里性价比最高的一步，应当立即做、独立做、先于 gate 补丁做。

#### **二、新事实 2：omniroute 的 token 计数与 NVIDIA 实测差 40%，"200000 软限 + 压缩"不构成防线**

生产 14:23:16 的日志暴露了一个你的分析没有展开的数字矛盾：omniroute 压缩后自认发送了 **212813** tokens（仍在它自己的 200000 软限之上），而 NVIDIA 实测同一请求为 **297040** tokens。偏差 84277，约 40%。可能的来源是 39 个 tools 的 schema 序列化口径、tokenizer 差异或压缩层的估算口径，但原因不影响结论：**omniroute 内部的 200000 软限和主动压缩（219223 → 212813，仅省 3%）在数学上无法保证不触 202752 硬顶**。这反过来加强了 (b) 的论据——gate 用字节标定（dev 弹 H：3900147B → 487511 tok；生产弹末日：同型 CJK 密集流量），直接锚定 NVIDIA 的实测口径 8 B/tok，而不是锚定 omniroute 那个偏 40% 的内部计数。real_context=200000 维持不变的结论依然成立，但它的定位要降级为"压缩触发器 Governor"（日志里 122083 阈值正是它派生的），不再被视为防 400 的盾。

#### **三、新事实 3："(a) 修一处两部署同愈"在部署拓扑上不成立**

两个部署跑的是不同的镜像与不同的 gate：生产 4.2.3 钉死官方 `diegosouzapw/omniroute:3.8.43@sha256:517c...` 加自带 gate.js（http-proxy-middleware 版）；dev 4.3.2 跑自建 `ghcr.io/i3t2y/omniroute-base:stable` 加 Dataset 下发的手写 http 代理版 gate.js。上游 auth.ts 在两者体内都是 Next.js 打包后的产物，改 src 意味着：4.3.2 侧要重建基镜像、推 GHCR、改 BASE_IMAGE、Rebuild Space；4.2.3 侧要 fork 官方镜像同样重建——两处重工程，且每次上游版本评估都要重新移植补丁。而 gate 补丁是两条各自小改、代码完全自有：4.3.2 侧走 Dataset push + **Restart 即生效（无需 Rebuild）**，4.2.3 侧改仓库重建。§5 风险为零。边际价值上，有了 (b) 加 4GB 堆之后，(a) 只剩下"灰区漏判时省掉 90 秒空转"这一点收益，正确处置是记录为上游 issue 加候选补丁（400 响应体含 `maximum context length` 时 exempt fallback），排进下一个基镜像周期，本轮不动。

#### **四、(b) 的落地规格（基于两份真实 gate 代码）**

拦截逻辑对两个 gate 同构，阈值全部 env 可调，便于后续按 gate 结构化日志里的估算字段持续标定：

| 区间（content-length） | 判定逻辑 | 动作 |
|---|---|---|
| < 600KB（`GATE_CTX_GRAY_FLOOR_B`） | 最病态内容也仅约 120K tok | 直接放行，零内存开销 |
| 600KB \~ 1.5MB | est = bytes ÷ 8（两起真实 400 的 NVIDIA 实测上界） | est > 195000 则 413，否则放行 |
| > 1.5MB（`GATE_CTX_HARD_B`） | 任何内容必然逼近或超过 202752 | 不读 body 直接 413 |

413 响应体带 `{type:"context_length_exceeded", est_tokens, limit:200000}`，CC/Hermes 视 4xx 为终态不重试。对 4.2.3 的 http-proxy-middleware 版，在 PSK 中间件后插一个 content-length 检查中间件即可。对 4.3.2 的手写代理版，插入点在 `/v1` PSK 中间件之后、`proxyV1` 之前；灰区需要缓冲 body（上限 1.5MB，有界），并在 proxyV1 的请求侧把 `req.pipe(upstreamReq)` 换成"若 `req._bufferedBody` 存在则 write 后 end"。你贴的 gate.js 恰好在 proxyV1 的响应流处理段被截断，请求侧 pipe 部分我看不到，**把 gate.js 尾部（proxyV1 函数剩余部分到文件尾）贴给我，我出行号锚定的完整 patch**。残余风险如实记录：1.1\~1.5MB 区间内的极端纯 CJK 文档可能漏判（实测 8 B/tok 上界对纯汉字 prose 偏乐观），漏判后果由 4096 堆加终态 exhaustion 兜底，(a) 落地后彻底闭合。

#### **五、立即执行项（独立小 patch，今天可做）**

在 omn-logic/entrypoint.sh 的上游启动行恢复证据支持的配置——用 4096 而非我之前拍脑袋的 6144，因为 4096 是生产同链验证过的值，SSOT 优先于推测：

```sh
cd /app
NODE_OPTIONS="${NODE_OPTIONS:---max-old-space-size=4096}" node server.js &
```

gate 进程轻量且流式，默认堆足够，可不加。

#### **六、audit 落证清单**

应落证八条：glm-5.2 硬顶 202752（两部署 400 原文互证）；real_context 维持 200000，定位修正为压缩 Governor 而非防 400 盾；#4 病链两部署同源（生产 14:23:16 与 dev 23:57:53 对照）；崩盘差异根因 = NODE_OPTIONS 4096 在 4.3.2 丢失（回归，非时机）；omniroute 计数与 NVIDIA 实测偏差 40%（212813 vs 297040），压缩不能作防线；qwen3.5-397b-a17b 在目录可查但 POST 探针 404——catalog 健康检查不等于可服务，池内可能注册了不可服务模型；生产 14:15 起 `ProxyFetch ECONNREFUSED 127.0.0.1:20129` 幽灵仍在（purge 显示 0/0/0 但运行时仍有连接尝试，脏 proxyUrl 存于他处，历史遗留）；4.2.3 init 在模型注册后有 `jq: Cannot index array with string "name"` 报错，需确认 4.3.2 是否已修。

把 gate.js 尾部贴来，我出最终 patch；需要的话我把 #4 全链与本次裁决存一条记忆，下轮免重建。

*内容由 AI 生成仅供参考*