# 3.8.48 dev boot snapshot 多帧实证闭环
# 径 C · 2026-07-27 · Zen笔2 验收闭环
# 出处: ops/STATUS.md 2026-07-27 段笔2 + commit 9a8acd2

## §0 闭环结论

笔2 (litestream snapshot 多帧语义) **全绿**:
- ① 代数递增 ✓ (两路证: evidence 双帧 0x0d→0x0f 跨两代 + 04:55Z 新 boot 全链 txid 0x01→0x1b 跨 27 代 monotonic)
- ② ERROR 定性 ✓ (K3 裁决修正: ERROR = entrypoint 空库恢复分支**设计行为**, **非** litestream 启动竞态 — ERROR 发生时 litestream 尚未初始化; 详见 §3)
- ③ 持续性多帧 ✓ (evidence 双帧隔 79min 见两代新快照 + 04:55Z boot 内 snapshot 持续到 txid 0x1b + compaction L0/L1/L2 多级活动)

dev 进 24h 观察 (起算 2026-07-27T05:31:55Z, 出门 2026-07-28T05:31:55Z)。

## §1 证据源

- workflow `fetch-nonoke-logs.yml` cron `*/30` 已在 nomn/main (sha b9da565e match) 自动每 30min 生帧, 写 evidence 分支 `logs/nonoke--omn/<TS>-run.log`
- fetch 语义: 单次抓取 = 全量 boot 历史积压 + 60s 实时窗 (非切片), 故多帧重叠含历史, 帧间新增流即新 snapshot 代
- 取最近两帧间隔 79min: 帧1 `20260727-0305-run.log` + 帧2 `20260727-0424-run.log`
- 本地 `git fetch nomn evidence` 通 (无 gh CLI 须要 — evidence 远端分支 git 直接可读)

## §2 snapshot 代数递增铁证

帧1 (03:05Z 抓) litestream 快照签名:
```
02:49:11.082Z litestream version=0.5.9 启
03:00:00.907Z snapshot complete txid=0x0d size=136787   ← 帧内最高代
```

帧2 (04:24Z 抓) litestream 快照签名:
```
03:00:00.907Z snapshot complete txid=0x0d size=136787   (帧1历史回放)
04:00:01.292Z snapshot complete txid=0x0f size=136842   ← 帧内最高代 (跨两代 0x0e→0x0f)
```

代数递增: 帧1末代 0x0d (13) → 帧2末代 0x0f (15), 79min 内 litestream 拍 ≥2 新快照 (0x0d→0x0e→0x0f), size 递增 +55B (136787→136842) 证真实 WAL 增量非空拍。

## §2.1 04:55Z 新 boot snapshot 全链证 (K3 全链 monotonic, 非抽样)

Zen贴完整 04:55:18Z boot log 内 litestream 启后全段 snapshot/compaction 代数链 (非 evidence 抽样双帧):

```
04:55:46.188Z litestream version=0.5.9 initialized
04:55:48.500Z snapshot complete txid=0x01 size=105383      ← 首代 (DB init 写完)
05:00:01.642Z snapshot complete txid=0x03 size=135704      ← compaction L2 合并
05:09:30.850Z L1 compaction txid 0x15→0x19 size=182562
05:11:00.924Z L1 compaction txid 0x1a→0x1b size=182563
05:15:02.075Z L2 compaction txid 0x1a→0x1b size=182563
05:15:46.627Z l0 retention deleted_count=23 max_l1_txid=0x1b
```

全链 monotonic 递增: txid 0x01 → 0x03 → 0x15→0x19 → 0x1a→0x1b, 跨 ≥27 代 (0x1b=27), size 增 105383→135704→182563 (**+77180B 真 WAL 增量**, 远超 evidence 双帧 +55B)。compaction L0/L1/L2 多级活动正常 (L0 retention 删 23 件 = L0 → L1 合并后续 maintenance 设计行为)。**非抽样双帧比, 是完整代数链全程 monotonic 递增** — 笔2 ①项"代数递增"从两帧铁证升级为全链铁证。

## §3 ERROR 定性 (K3 裁决修正 — entrypoint 空库恢复分支, 设计行为)

### §3.1 evidence 双帧旧 boot (02:48Z)

全帧单 ERROR:
```
02:49:04.946Z level=ERROR "failed to run" database not found in config: /data/storage.sqlite
```

### §3.2 04:55Z 新 boot 完整时序 (K3 裁决核心证据)

```
04:55:40.010Z level=ERROR "failed to run" error="database not found in config: /data/storage.sqlite"
04:55:40.xxx  [entrypoint] ⚠ restore 失败 rc=1 ... 空库启动
04:55:46.188Z litestream version=0.5.9 initialized  ← 比 ERROR 晚 6 秒
04:55:48.500Z level=INFO snapshot complete txid=0x01 size=105383   ← 首 snapshot 成功
```

### §3.3 K3 裁决: 非 litestream 启动竞态, 是 entrypoint 恢复逻辑正常分支

时序铁证 (litestream 初始化 04:55:46 **晚于** ERROR 04:55:40 整 6 秒):
- ERROR 发生时 litestream 进程**尚未初始化**, 无可能产生"启动竞态"
- 真序: R2 无快照 (或 LITESTREAM_REPLICA_URL 指向空桶) → litestream restore 尝试 (设计行为, 非崩溃) → restore 失败 ERROR → entrypoint 捕获 rc=1 明确决策"空库启动" → OmniRoute 跑 118 件迁移建库 → litestream 04:55:46 才初始化 → snapshot complete txid=0x01 首代成功

**反证闭环**: txid 从 0x01 → 0x03 → ... → 0x1b 持续 monotonic 递增 (§2.1 全链条), 证配置正确无持续故障。若真 DB 配错 litestream 拍不了快照。

### §3.4 性质修正 (旧稿"litestream 启动竞态 transient"措辞作废)

| 维度 | 旧稿 (作废) | K3 裁决 (现稿) |
|---|---|---|
| ERROR 源 | "litestream 进程启动前 DB init 竞态" | "entrypoint 空库恢复分支设计行为 (R2 无快照 → restore 失败 → 空库启动)" |
| ERROR 时序 | "02:49:11 litestream 正启+DB 配置就绪反证" | "litestream 04:55:46 初始化, **晚于** ERROR 04:55:40 整 6 秒 — litestream 不在场无竞态可能" |
| 标准达标 | "快照活动层零 ERROR" | 同左 + "恢复流程 ERROR 乃设计分支非故障" |

### §3.5 首启 vs 重启鉴别 (需Zen裁确认 — dev 进窗观察的潜在风险点)

K3 提醒关键鉴别: 若此 04:55 boot **首次部署 3.8.48** (greenfield), R2 无快照 = 正常空库启动 ✓。
若此 boot **是重启** (非首次), R2 应有旧快照却 restore 失败 = R2 桶里为何无快照须查 (R2 配置/桶权限/快照删除)。

判此为 24h 窗内监控附加项: 见 nonoke/omn 重启即向Zen贴回判别首启/重启 + R2 restore 行为。

### §3.6 日志两处真异常入档 (与 3.8.48 无关, 记录不阻塞, 须排除验收标准外)

**异常 1: 测试流量混入 dev + "真id" 字面量占位符请求**

日志 05:01 时段见:
```
{"method":"POST","path":"/v1/chat/completions | 真id | 1 msgs"...
{"msg":"Unable to determine provider for model '真id'"...
```

有客户端 (cg52 测试脚本 / Zen手 curl — 早先Zen手粘 model:'真id' 见 §6 笔1 探针首跑 HTTP400 实证) 发送字面量"真id"作 model 名 → 测试流量混入 dev 环境。

性质: 不阻塞, 非系统故障。但须 STATUS 标注"05:01 时段测试流量含无效 model id '真id', 已排除验收标准外", 防未来检索误判为 3.8.48 回归。

**异常 2: 上游 opencode.ai 大面积 403 (Cloudflare Error 1010) 触发 circuit breaker**

auto/best-coding combo 05:07-05:08 遍历 34 模型, opencode 连续 5 次 403 触发 CB:
```
🔴 Circuit breaker tripped for opencode: 5 consecutive failures. Blocked for 30min.
```

性质: 与 3.8.48 **完全无关** — 上游 provider 访问控制 (Cloudflare Error 1010 浏览器签名 ban), 非 OmniRoute 代码问题。导致闸③测试时 combo 池降级 (从 61 模型缩到后续 deepseek-v4-flash-free 命中)。dev 环境网络/上游限制, 非本次升级引入回归。

两异常皆入档知悉, 不阻 dev 进 24h 窗, 不作 3.8.48 验收扣分项。



## §4 语义核对 (Zen笔2 要求)

- ① 代数递增: §2 evidence 双帧 0x0d→0x0f + §2.1 04:55 boot 全链 0x01→0x1b ✓
- ② 零 litestream ERROR: 快照活动层零 error; §3 现定性 ERROR = entrypoint 空库恢复分支设计行为 (litestream 未在场无竞态可能), 非故障 ✓
- ③ 现产出快照 = 迁移后快照 (boot 02:48Z 后 113-122 迁移全跑完, snapshot @03:00/04:00 为迁移后态) ✓
- ④ nomke prod 回滚锚点 = 切换前最后一帧 (dev 此两帧不直接作 prod 锚, prod 切前须另抓 nomke 切换前帧)

## §5 24h 窗起算

- 笔1 绿末: 2026-07-27T05:09:36Z (gate 413 三道防伪闸全绿)
- 笔2 绿末: 2026-07-27T05:31:55Z (snapshot 多帧三判全绿)
- 24h 窗起算 = max(05:09:36Z, 05:31:55Z) = **2026-07-27T05:31:55Z**
- 24h 出门 = **2026-07-28T05:31:55Z**

## §6 出门标准 (Zen钉死, 见 ops/STATUS.md)

24h 窗满四点全绿判 dev 过门推 prod:
1. gate 413 三道防伪闸持续全绿 (含 413 双铁证: body 实超 1.6+MB/413 归属 gate 签) + snapshot 多帧
2. 日志零 ERROR 零 crash loop 非预期重启 (§3 entrypoint 空库恢复分支 ERROR = 设计行为非故障, 不算阻)
3. 25-key 周期探活持续全绿
4. litestream snapshot 节奏正常无断档 (每 30min sync-interval 一代)

## §7 prod 切前三 checklist (Zen下令时)

1. nomke/omn 此刻仍 3.8.43 验 (确认未提前漂)
2. nonoke 死 BASE_IMAGE Variable 了断 (清除 Space 侧陈旧 Variable 防未来钻 ARG 默认值)
3. nomke 补设 EXPECTED_VERSION=3.8.48 升 boot 硬断言 (不留"期望未设置跳过比对"口子)

prod 切: Zen workflow_dispatch sync-space-nomke.yml 显令 + 盯 R2 restore 拉 omniroute-data 真库 (restore 失败即停 数据丢失风险) + boot 三硬标 (版本=3.8.48/bucket=omniroute-data 不变/snapshot complete) + prod 侧 compaction/AccessDenied 见一即停。

## §8 纪律守

笔2 全程:
- 未 dispatch 任何 workflow (cron 已自动, 无须启动手续)
- 未触 Space (dev env urllib 笔1 是笔1非笔2)
- 零 commit 改 (笔2 不改 workflow — cron 已存在 sha match)
- evidence 分支只读 grep (git fetch nomn evidence 非触)

静候Zen判 dev 进 24h 观察 + 24h 窗满后五步序推 nomke。

## §9 K3 审计裁决闭环 (Zen 2026-07-27 同步令)

K3 (审计员 Kimi-K3) 对Zen贴 04:55:18Z 完整 boot log 的分析裁决, Zen同步令 cg52 落账:

**裁决要点**:
1. ERROR 定性修正: "entrypoint 空库恢复分支 (设计行为)"取代旧"litestream 启动竞态" — litestream 04:55:46 初始化晚于 ERROR 04:55:40 整 6 秒, litestream 不在场无竞态可能 (§3 已落账)
2. 两处真异常入档 (§3.6):
   - 05:01 "真id" 测试流量混入 (无效样本, STATUS 须标注排除验收外)
   - 05:07-05:08 opencode Cloudflare 403/Error 1010 触发 CB (上游限制非 3.8.48 回归)
3. 24h 窗状态确认: 起算 05:31:55Z (笔2 绿末, txid 0x0f) / 到期 07-28T05:31:55Z / 窗内监控标准 = 每 30min 验 evidence 新帧 txid 递增 + 零新增 ERROR
4. 静候令: 窗满前禁任何 nomke 操作; 窗满后生成 prod 切换 checklist 供Zen裁决

**本轮同步令产出**:
- §3 ERROR 定性全面修正 (K3 裁决核心)
- §2.1 04:55 boot snapshot 全链铁证追加 (代数 0x01→0x1b 全程 monotonic)
- §3.6 两异常入档
- §4 ② 项措辞 sync §3 现定性
- §6 ② 项出门标准 sync §3

零代码变更, 纯文档落账。
